# Enable accessible disabled buttons to show tooltips on disabled controls
# This allows disabled buttons (e.g., self-delete guard in UserManagementView) to be
# focusable/hoverable, so their explanatory tooltips render. They still cannot be triggered.
com.vaadin.experimental.accessibleDisabledButtons=true
