import { o as css } from "./reactive-element-CKo3LdqU.js";
import { n as html } from "./lit-html-x1Rck0rJ.js";
import { LitElement } from "./lit.js";
import { t as PolylitMixin } from "./polylit-mixin-BwU08z1T.js";
import { defineCustomElement } from "./@vaadin_component-base_src_define__js.js";
import { t as LumoInjectionMixin } from "./lumo-injection-mixin-CJJZE4Xo.js";
import { t as ThemableMixin } from "./vaadin-themable-mixin-CYKnTaeR.js";
import "./style-props-Bqqs274Y.js";
import { t as ElementMixin } from "./element-mixin-B8J2EQHC.js";
import { t as getNormalizedScrollLeft } from "./dir-utils-9YTU0Dgy.js";
import { t as ListMixin } from "./list-mixin-u1NpsUbs.js";
import { t as ResizeMixin } from "./resize-mixin-CgzqQOqA.js";
import "./vaadin-tab-CWJ6i2A1.js";
//#region node_modules/@vaadin/tabs/src/styles/vaadin-tabs-base-styles.js
/**
* @license
* Copyright (c) 2017 - 2026 Vaadin Ltd.
* This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
*/
var tabsStyles = css`
  :host {
    display: flex;
    max-width: 100%;
    max-height: 100%;
    position: relative;
    box-sizing: border-box;
    padding: var(--vaadin-tabs-padding);
    background: var(--vaadin-tabs-background);
    border-radius: var(--vaadin-tabs-border-radius);
    border: var(--vaadin-tabs-border-width, 0) solid
      var(--vaadin-tabs-border-color, var(--vaadin-border-color-secondary));
  }

  :host([hidden]) {
    display: none !important;
  }

  :host([orientation='vertical']) {
    flex-direction: column;
  }

  [part='tabs'] {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: var(--vaadin-tabs-gap, var(--vaadin-gap-s));
    animation: enable-smooth-scroll-after-first-render 1s both;
    width: 100%;
    height: 100%;
  }

  :host([overflow]) [part='tabs'] {
    overflow: auto;
  }

  :host([overflow][orientation='horizontal']) [part='tabs'] {
    overscroll-behavior: contain auto;
  }

  :host([overflow][orientation='vertical']) [part='tabs'] {
    overscroll-behavior: auto contain;
  }

  @keyframes enable-smooth-scroll-after-first-render {
    100% {
      scroll-behavior: smooth;
    }
  }

  :host([orientation='horizontal']) [part='tabs'] {
    flex-direction: row;
    scrollbar-width: none;
  }

  /* scrollbar-width is supported in Safari 18.2, use the following for earlier */
  :host([orientation='horizontal']) [part='tabs']::-webkit-scrollbar {
    display: none;
  }

  [part$='button'] {
    position: absolute;
    z-index: 1;
    pointer-events: none;
    opacity: 0;
    cursor: var(--vaadin-clickable-cursor);
    box-sizing: border-box;
    height: 100%;
    padding: var(--vaadin-tab-padding, var(--vaadin-padding-block-container) var(--vaadin-padding-inline-container));
    background: var(--vaadin-background-color);
    display: flex;
    align-items: center;
    justify-content: center;
    -webkit-tap-highlight-color: transparent;
    -webkit-user-select: none;
    user-select: none;
    touch-action: manipulation;
  }

  [part='forward-button'] {
    inset-inline-end: 0;
  }

  :host([overflow~='start']) [part='back-button'],
  :host([overflow~='end']) [part='forward-button'] {
    pointer-events: auto;
    opacity: 1;
  }

  [part$='button']::before {
    content: '';
    display: block;
    width: var(--vaadin-icon-size, 1lh);
    height: var(--vaadin-icon-size, 1lh);
    background: currentColor;
    mask: var(--_vaadin-icon-chevron-down) 50% / var(--vaadin-icon-visual-size, 100%) no-repeat;
    rotate: 90deg;
  }

  [part='forward-button']::before {
    rotate: -90deg;
  }

  :host(:is([orientation='vertical'], [theme~='hide-scroll-buttons'])) [part$='button'] {
    display: none;
  }

  @media (pointer: coarse) {
    :host(:not([theme~='show-scroll-buttons'])) [part$='button'] {
      display: none;
    }
  }

  :host([dir='rtl']) [part$='button']::before {
    scale: 1 -1;
  }

  @media (forced-colors: active) {
    [part$='button']::before {
      background: CanvasText;
    }
  }
`;
//#endregion
//#region node_modules/@vaadin/tabs/src/vaadin-tabs-mixin.js
/**
* @license
* Copyright (c) 2017 - 2026 Vaadin Ltd.
* This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
*/
var TabsMixin = (superClass) => class TabsMixinClass extends ResizeMixin(ListMixin(superClass)) {
	static get properties() {
		return {
			/**
			* Set tabs disposition. Possible values are `horizontal|vertical`
			*/
			orientation: {
				value: "horizontal",
				type: String,
				reflectToAttribute: true,
				sync: true
			},
			/**
			* The index of the selected tab.
			*/
			selected: {
				value: 0,
				type: Number,
				reflectToAttribute: true
			}
		};
	}
	static get observers() {
		return ["__tabsItemsChanged(items)"];
	}
	constructor() {
		super();
		this.__itemsResizeObserver = new ResizeObserver(() => {
			setTimeout(() => this._updateOverflow());
		});
	}
	/**
	* @return {number}
	* @protected
	*/
	get _scrollOffset() {
		return this._vertical ? this._scrollerElement.offsetHeight : this._scrollerElement.offsetWidth;
	}
	/**
	* @return {!HTMLElement}
	* @protected
	* @override
	*/
	get _scrollerElement() {
		return this.$.scroll;
	}
	/** @private */
	get __direction() {
		return !this._vertical && this.__isRTL ? 1 : -1;
	}
	/** @protected */
	ready() {
		super.ready();
		this._updateOverflow();
		this._scrollerElement.addEventListener("scroll", () => this._updateOverflow());
		this.setAttribute("role", "tablist");
	}
	/**
	* @protected
	* @override
	*/
	_onResize() {
		this._updateOverflow();
	}
	/** @private */
	__tabsItemsChanged(items) {
		this.__itemsResizeObserver.disconnect();
		(items || []).forEach((item) => {
			this.__itemsResizeObserver.observe(item);
		});
		this._updateOverflow();
	}
	_scrollToItem(idx) {
		const item = this.items[idx];
		const itemRect = item.getBoundingClientRect();
		const scrollerRect = this._scrollerElement.getBoundingClientRect();
		const overflowIndicatorCompensation = this._vertical ? 10 : 20;
		if (this._vertical) {
			if (itemRect.bottom > scrollerRect.bottom - overflowIndicatorCompensation) this._scrollerElement.scrollTop = item.offsetTop - (scrollerRect.height - itemRect.height) + overflowIndicatorCompensation;
			if (itemRect.top < scrollerRect.top + overflowIndicatorCompensation) this._scrollerElement.scrollTop = item.offsetTop - overflowIndicatorCompensation;
		} else {
			const backButtonWidth = this.shadowRoot.querySelector(`[part="back-button"]`).offsetWidth;
			const forwardButtonWidth = this.shadowRoot.querySelector(`[part="forward-button"]`).offsetWidth;
			if (itemRect.right > scrollerRect.right - forwardButtonWidth - overflowIndicatorCompensation) this._scrollerElement.scrollLeft = item.offsetLeft - (scrollerRect.width - itemRect.width) + forwardButtonWidth + overflowIndicatorCompensation;
			if (itemRect.left < scrollerRect.left + backButtonWidth + overflowIndicatorCompensation) this._scrollerElement.scrollLeft = item.offsetLeft - backButtonWidth - overflowIndicatorCompensation;
		}
	}
	/** @protected */
	_scrollForward(e) {
		if (e === void 0 || this.__scrollTimer === void 0) this._scroll(this.__direction * (this._scrollOffset / 2) * -1);
	}
	/** @protected */
	_scrollBack(e) {
		if (e === void 0 || this.__scrollTimer === void 0) this._scroll(this.__direction * (this._scrollOffset / 2));
	}
	/** @protected */
	_startScrollForward(e) {
		if (e.button === 0) {
			this._scrollForward();
			this.__scrollTimer = setInterval(this._scrollForward.bind(this), 300);
		}
	}
	/** @protected */
	_startScrollBack(e) {
		if (e.button === 0) {
			this._scrollBack();
			this.__scrollTimer = setInterval(this._scrollBack.bind(this), 300);
		}
	}
	/** @protected */
	_stopScroll() {
		clearTimeout(this.__scrollTimer);
	}
	/** @private */
	_updateOverflow() {
		const scrollPosition = this._vertical ? this._scrollerElement.scrollTop : getNormalizedScrollLeft(this._scrollerElement, this.getAttribute("dir"));
		const scrollSize = this._vertical ? this._scrollerElement.scrollHeight : this._scrollerElement.scrollWidth;
		let overflow = Math.floor(scrollPosition) > 1 ? "start" : "";
		if (Math.ceil(scrollPosition) < Math.ceil(scrollSize - this._scrollOffset)) overflow += " end";
		if (this.__direction === 1) overflow = overflow.replace(/start|end/giu, (matched) => {
			return matched === "start" ? "end" : "start";
		});
		if (overflow) this.setAttribute("overflow", overflow.trim());
		else this.removeAttribute("overflow");
	}
};
//#endregion
//#region node_modules/@vaadin/tabs/src/vaadin-tabs.js
/**
* @license
* Copyright (c) 2017 - 2026 Vaadin Ltd.
* This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
*/
/**
* `<vaadin-tabs>` is a Web Component for organizing and grouping content into sections.
*
* ```html
* <vaadin-tabs selected="4">
*   <vaadin-tab>Page 1</vaadin-tab>
*   <vaadin-tab>Page 2</vaadin-tab>
*   <vaadin-tab>Page 3</vaadin-tab>
*   <vaadin-tab>Page 4</vaadin-tab>
* </vaadin-tabs>
* ```
*
* ### Styling
*
* The following shadow DOM parts are available for styling:
*
* Part name         | Description
* ------------------|--------------------------------------
* `back-button`     | Button for moving the scroll back
* `tabs`            | The tabs container
* `forward-button`  | Button for moving the scroll forward
*
* The following state attributes are available for styling:
*
* Attribute      | Description
* ---------------|--------------------------------------
* `orientation`  | Tabs disposition, valid values are `horizontal` and `vertical`
* `overflow`     | It's set to `start`, `end`, none or both.
*
* The following custom CSS properties are available for styling:
*
* Custom CSS property              |
* :--------------------------------|
* | `--vaadin-tabs-background`     |
* | `--vaadin-tabs-border-color`   |
* | `--vaadin-tabs-border-radius`  |
* | `--vaadin-tabs-border-width`   |
* | `--vaadin-tabs-font-size`      |
* | `--vaadin-tabs-font-weight`    |
* | `--vaadin-tabs-gap`            |
* | `--vaadin-tabs-padding`        |
*
* See [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation.
*
* @fires {CustomEvent} items-changed - Fired when the `items` property changes.
* @fires {CustomEvent} selected-changed - Fired when the `selected` property changes.
*
* @customElement vaadin-tabs
* @extends HTMLElement
*/
var Tabs = class extends TabsMixin(ElementMixin(ThemableMixin(PolylitMixin(LumoInjectionMixin(LitElement))))) {
	static get is() {
		return "vaadin-tabs";
	}
	static get styles() {
		return tabsStyles;
	}
	/** @protected */
	render() {
		return html`
      <div
        @pointerdown="${this._startScrollBack}"
        @pointerup="${this._stopScroll}"
        @pointerleave="${this._stopScroll}"
        @pointercancel="${this._stopScroll}"
        @click="${this._scrollBack}"
        part="back-button"
        aria-hidden="true"
      ></div>

      <div id="scroll" part="tabs" tabindex="-1">
        <slot></slot>
      </div>

      <div
        @pointerdown="${this._startScrollForward}"
        @pointerup="${this._stopScroll}"
        @pointerleave="${this._stopScroll}"
        @pointercancel="${this._stopScroll}"
        @click="${this._scrollForward}"
        part="forward-button"
        aria-hidden="true"
      ></div>
    `;
	}
};
defineCustomElement(Tabs);
//#endregion
export { Tabs as t };

//# sourceMappingURL=vaadin-tabs-DbtelXDi.js.map