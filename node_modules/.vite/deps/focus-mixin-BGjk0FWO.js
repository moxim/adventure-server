import { i as dedupeMixin } from "./polylit-mixin-BwU08z1T.js";
import { o as isKeyboardActive } from "./focus-utils-CzjQXswF.js";
//#region node_modules/@vaadin/a11y-base/src/focus-mixin.js
/**
* @license
* Copyright (c) 2021 - 2026 Vaadin Ltd.
* This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
*/
/**
* A mixin to handle `focused` and `focus-ring` attributes based on focus.
*/
var FocusMixinImplementation = (superclass) => {
	return class FocusMixinClass extends superclass {
		/**
		* @protected
		* @return {boolean}
		*/
		get _keyboardActive() {
			return isKeyboardActive();
		}
		/** @protected */
		ready() {
			this.addEventListener("focusin", (e) => {
				if (this._shouldSetFocus(e)) this._setFocused(true);
			});
			this.addEventListener("focusout", (e) => {
				if (this._shouldRemoveFocus(e)) this._setFocused(false);
			});
			super.ready();
		}
		/** @protected */
		disconnectedCallback() {
			super.disconnectedCallback();
			if (this.hasAttribute("focused")) this._setFocused(false);
		}
		/**
		* @param {FocusOptions=} options
		* @protected
		* @override
		*/
		focus(options) {
			super.focus(options);
			if (options?.focusVisible !== false) this.setAttribute("focus-ring", "");
		}
		/**
		* Override to change how focused and focus-ring attributes are set.
		*
		* @param {boolean} focused
		* @protected
		*/
		_setFocused(focused) {
			this.toggleAttribute("focused", focused);
			this.toggleAttribute("focus-ring", focused && this._keyboardActive);
		}
		/**
		* Override to define if the field receives focus based on the event.
		*
		* @param {FocusEvent} _event
		* @return {boolean}
		* @protected
		*/
		_shouldSetFocus(_event) {
			return true;
		}
		/**
		* Override to define if the field loses focus based on the event.
		*
		* @param {FocusEvent} _event
		* @return {boolean}
		* @protected
		*/
		_shouldRemoveFocus(_event) {
			return true;
		}
	};
};
var FocusMixin = dedupeMixin(FocusMixinImplementation);
//#endregion
export { FocusMixin as t };

//# sourceMappingURL=focus-mixin-BGjk0FWO.js.map