import { l as unsafeCSS, o as css } from "./reactive-element-CKo3LdqU.js";
import "./lit.js";
import "./style-props-Bqqs274Y.js";
import { t as SlotChildObserveController } from "./slot-child-observe-controller-DRwVKtF7.js";
//#region node_modules/@vaadin/details/src/styles/vaadin-details-summary-base-styles.js
/**
* @license
* Copyright (c) 2025 - 2026 Vaadin Ltd.
* This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
*/
var detailsSummary = (partName = "vaadin-details-summary") => css`
  :host {
    align-items: center;
    background: var(--${unsafeCSS(partName)}-background, transparent);
    background-origin: border-box;
    border: var(--${unsafeCSS(partName)}-border-width, 0) solid
      var(--${unsafeCSS(partName)}-border-color, var(--vaadin-border-color-secondary));
    border-radius: var(--${unsafeCSS(partName)}-border-radius, var(--vaadin-radius-m));
    box-sizing: border-box;
    color: var(--${unsafeCSS(partName)}-text-color, var(--vaadin-text-color));
    cursor: var(--vaadin-clickable-cursor);
    display: flex;
    font-size: var(--${unsafeCSS(partName)}-font-size, inherit);
    font-weight: var(--${unsafeCSS(partName)}-font-weight, 500);
    gap: var(--${unsafeCSS(partName)}-gap, 0 var(--vaadin-gap-s));
    height: var(--${unsafeCSS(partName)}-height, auto);
    outline: calc(var(--vaadin-focus-ring-width) * var(--_focus-ring, 0)) solid var(--vaadin-focus-ring-color);
    outline-offset: 1px;
    padding: var(
      --${unsafeCSS(partName)}-padding,
      var(--vaadin-padding-block-container) var(--vaadin-padding-inline-container)
    );
    -webkit-tap-highlight-color: transparent;
    -webkit-user-select: none;
    user-select: none;
  }

  :host([focus-ring]) {
    --_focus-ring: 1;
  }

  :host([hidden]) {
    display: none !important;
  }

  [part='toggle'] {
    color: var(--vaadin-text-color-secondary);
  }

  @media (prefers-reduced-motion: no-preference) {
    [part='toggle'] {
      transition-property: rotate;
      transition-duration: 150ms;
      animation: delay-initial-transition 1ms;
    }

    @keyframes delay-initial-transition {
      0% {
        rotate: 0deg;
      }
    }
  }

  [part='toggle']::before {
    background: currentColor;
    content: '';
    display: block;
    height: var(--vaadin-icon-size, 1lh);
    mask: var(--_vaadin-icon-chevron-down) 50% / var(--vaadin-icon-visual-size, 100%) no-repeat;
    width: var(--vaadin-icon-size, 1lh);
    rotate: -90deg;
  }

  :host([disabled]) {
    opacity: 0.5;
    cursor: var(--vaadin-disabled-cursor);
  }

  :host([dir='rtl']) [part='toggle']::before {
    scale: -1;
  }

  :host([opened]) [part='toggle'] {
    rotate: 90deg;
  }

  :host([dir='rtl'][opened]) [part='toggle'] {
    rotate: -90deg;
  }

  @media (forced-colors: active) {
    [part='toggle']::before {
      background: CanvasText;
    }

    :host([disabled]) {
      color: GrayText;
      opacity: 1;
    }

    :host([disabled]) [part='toggle']::before {
      background: GrayText;
    }
  }
`;
//#endregion
//#region node_modules/@vaadin/details/src/content-controller.js
/**
* @license
* Copyright (c) 2019 - 2026 Vaadin Ltd.
* This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
*/
/**
* A controller to manage the default content slot.
*/
var ContentController = class extends SlotChildObserveController {
	/**
	* Override method from `SlotController` to change
	* the ID prefix for the default slot content.
	*
	* @param {HTMLElement} host
	* @return {string}
	* @protected
	* @override
	*/
	static generateId(host) {
		return super.generateId(host, "content");
	}
	constructor(host) {
		super(host, "", null, { multiple: true });
	}
};
//#endregion
//#region node_modules/@vaadin/details/src/collapsible-mixin.js
/**
* @license
* Copyright (c) 2019 - 2026 Vaadin Ltd.
* This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
*/
/**
* A mixin providing common functionality for making content collapsible,
* used by `<vaadin-details>` and `<vaadin-accordion-panel>` elements.
*/
var CollapsibleMixin = (superClass) => class CollapsibleMixinClass extends superClass {
	static get properties() {
		return {
			/**
			* If true, the collapsible content is visible.
			*/
			opened: {
				type: Boolean,
				value: false,
				reflectToAttribute: true,
				notify: true
			},
			/**
			* List of elements assigned to the default `<slot>`
			* that represent the collapsible content.
			*
			* @protected
			*/
			_contentElements: { type: Array }
		};
	}
	static get observers() {
		return ["_openedOrContentChanged(opened, _contentElements)"];
	}
	constructor() {
		super();
		this._contentController = new ContentController(this);
		this._contentController.addEventListener("slot-content-changed", (event) => {
			const content = event.target.nodes || [];
			this._contentElements = content.filter((node) => node.parentNode === this);
		});
	}
	/** @protected */
	ready() {
		super.ready();
		this.addController(this._contentController);
		this.addEventListener("click", ({ target }) => {
			if (this.disabled) return;
			if (target.localName === "a") return;
			const summary = this.focusElement;
			if (summary && (target === summary || summary.contains(target))) this.opened = !this.opened;
		});
	}
	/** @private */
	_openedOrContentChanged(opened, elements) {
		if (elements) elements.forEach((el) => {
			el.setAttribute("aria-hidden", opened ? "false" : "true");
		});
	}
};
//#endregion
//#region node_modules/@vaadin/details/src/summary-controller.js
/**
* @license
* Copyright (c) 2019 - 2026 Vaadin Ltd.
* This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
*/
/**
* A controller to manage the summary element.
*/
var SummaryController = class extends SlotChildObserveController {
	constructor(host, tagName) {
		super(host, "summary", tagName);
	}
	/**
	* Set summary based on corresponding host property.
	*
	* @param {string} summary
	*/
	setSummary(summary) {
		this.summary = summary;
		if (!this.getSlotChild()) this.restoreDefaultNode();
		if (this.node === this.defaultNode) this.updateDefaultNode(this.node);
	}
	/**
	* Override method inherited from `SlotChildObserveController`
	* to restore and observe the default summary element.
	*
	* @protected
	* @override
	*/
	restoreDefaultNode() {
		const { summary } = this;
		if (summary && summary.trim() !== "") this.attachDefaultNode();
	}
	/**
	* Override method inherited from `SlotChildObserveController`
	* to update the default summary element text content.
	*
	* @param {Node | undefined} node
	* @protected
	* @override
	*/
	updateDefaultNode(node) {
		if (node) node.textContent = this.summary;
		super.updateDefaultNode(node);
	}
};
//#endregion
export { CollapsibleMixin as n, detailsSummary as r, SummaryController as t };

//# sourceMappingURL=summary-controller-lQ2JtnTe.js.map