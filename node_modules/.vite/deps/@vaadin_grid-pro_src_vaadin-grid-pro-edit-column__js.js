import { n as get, r as set } from "./polylit-mixin-BwU08z1T.js";
import { defineCustomElement } from "./@vaadin_component-base_src_define__js.js";
import { t as getDeepActiveElement } from "./focus-utils-CzjQXswF.js";
import { t as Checkbox } from "./vaadin-checkbox-CD8g5iNv.js";
import { GridColumn } from "./@vaadin_grid_src_vaadin-grid-column__js.js";
import { t as TextField } from "./vaadin-text-field-CttvI2rY.js";
import { t as Select } from "./vaadin-select-F0FH20gK.js";
//#region node_modules/@vaadin/grid-pro/src/vaadin-grid-pro-edit-checkbox.js
/**
* @license
* Copyright (c) 2000 - 2026 Vaadin Ltd.
*
* This program is available under Vaadin Commercial License and Service Terms.
*
*
* See https://vaadin.com/commercial-license-and-service-terms for the full
* license.
*/
/**
* An element used internally by `<vaadin-grid-pro>`. Not intended to be used separately.
*
* @customElement vaadin-grid-pro-edit-checkbox
* @extends Checkbox
* @private
*/
var GridProEditCheckbox = class extends Checkbox {
	static get is() {
		return "vaadin-grid-pro-edit-checkbox";
	}
};
defineCustomElement(GridProEditCheckbox);
//#endregion
//#region node_modules/@vaadin/grid-pro/src/vaadin-grid-pro-edit-select-mixin.js
/**
* @license
* Copyright (c) 2000 - 2026 Vaadin Ltd.
*
* This program is available under Vaadin Commercial License and Service Terms.
*
* See https://vaadin.com/commercial-license-and-service-terms for the full
* license.
*/
var GridProEditSelectMixin = (superClass) => class extends superClass {
	static get properties() {
		return {
			options: {
				type: Array,
				value: () => []
			},
			_grid: { type: Object },
			_initialized: { type: Boolean }
		};
	}
	static get observers() {
		return ["_optionsChanged(options)"];
	}
	ready() {
		super.ready();
		this.setAttribute("theme", "grid-pro-editor");
	}
	_onKeyDown(e) {
		super._onKeyDown(e);
		if (this.options.length === 0 && /^(ArrowDown|Down|ArrowUp|Up|Enter|SpaceBar| )$/u.test(e.key)) console.warn("Missing \"editorOptions\" for <vaadin-grid-pro-edit-column> select editor!");
		if (e.defaultPrevented) e.stopPropagation();
	}
	/**
	* Override list-box event listener inherited from `Select`:
	* - Enter: set flag for moving to next row on value change,
	* - Tab: switch to next cell when "singleCellEdit" is false.
	* @param {!KeyboardEvent} event
	* @protected
	* @override
	*/
	_onKeyDownInside(event) {
		if (event.keyCode === 13) this._enterKeydown = event;
		if (event.keyCode === 9) {
			if (!this._grid.singleCellEdit) this._grid._switchEditCell(event);
		}
		super._onKeyDownInside(event);
	}
	_valueChanged(value, oldValue) {
		super._valueChanged(value, oldValue);
		if (value === "" && oldValue === void 0) return;
		if (this._initialized) {
			const enter = this._enterKeydown;
			if (enter && this._grid.enterNextRow) this._grid._switchEditCell(enter);
			else if (this._grid.singleCellEdit) this._grid._stopEdit(false, true);
			else this.focus();
		}
	}
	_optionsChanged(options) {
		if (options?.length) {
			this.items = options.map((option) => ({
				label: option,
				value: option
			}));
			this._overlayElement ||= this.shadowRoot.querySelector("vaadin-select-overlay");
			this._overlayElement.addEventListener("vaadin-overlay-outside-click", () => {
				this._grid._stopEdit();
			});
			setTimeout(() => {
				this.opened = true;
				this._initialized = true;
			});
		}
	}
};
//#endregion
//#region node_modules/@vaadin/grid-pro/src/vaadin-grid-pro-edit-select.js
/**
* @license
* Copyright (c) 2000 - 2026 Vaadin Ltd.
*
* This program is available under Vaadin Commercial License and Service Terms.
*
* See https://vaadin.com/commercial-license-and-service-terms for the full
* license.
*/
/**
* An element used internally by `<vaadin-grid-pro>`. Not intended to be used separately.
*
* @customElement vaadin-grid-pro-edit-select
* @extends Select
* @private
*/
var GridProEditSelect = class extends GridProEditSelectMixin(Select) {
	static get is() {
		return "vaadin-grid-pro-edit-select";
	}
};
defineCustomElement(GridProEditSelect);
//#endregion
//#region node_modules/@vaadin/grid-pro/src/vaadin-grid-pro-edit-text-field.js
/**
* @license
* Copyright (c) 2000 - 2026 Vaadin Ltd.
*
* This program is available under Vaadin Commercial License and Service Terms.
*
*
* See https://vaadin.com/commercial-license-and-service-terms for the full
* license.
*/
/**
* An element used internally by `<vaadin-grid-pro>`. Not intended to be used separately.
*
* @customElement vaadin-grid-pro-edit-text-field
* @extends TextField
* @private
*/
var GridProEditText = class extends TextField {
	static get is() {
		return "vaadin-grid-pro-edit-text-field";
	}
	ready() {
		super.ready();
		this.setAttribute("theme", "grid-pro-editor");
	}
};
defineCustomElement(GridProEditText);
//#endregion
//#region node_modules/@vaadin/grid-pro/src/vaadin-grid-pro-edit-column-mixin.js
/**
* @license
* Copyright (c) 2000 - 2026 Vaadin Ltd.
*
* This program is available under Vaadin Commercial License and Service Terms.
*
* See https://vaadin.com/commercial-license-and-service-terms for the full
* license.
*/
var GridProEditColumnMixin = (superClass) => class extends superClass {
	static get properties() {
		return {
			/**
			* Custom function for rendering the cell content in edit mode.
			* Receives three arguments:
			*
			* - `root` The cell content DOM element. Append your editor component to it.
			* - `column` The `<vaadin-grid-pro-edit-column>` element.
			* - `model` The object with the properties related with
			*   the rendered item, contains:
			*   - `model.index` The index of the item.
			*   - `model.item` The item.
			*   - `model.expanded` Sublevel toggle state.
			*   - `model.level` Level of the tree represented with a horizontal offset of the toggle button.
			*   - `model.selected` Selected state.
			*   - `model.detailsOpened` Details opened state.
			* @type {!GridBodyRenderer | null | undefined}
			*/
			editModeRenderer: {
				type: Function,
				sync: true
			},
			/**
			* The list of options which should be passed to cell editor component.
			* Used with the `select` editor type, to provide a list of items.
			* @type {!Array<string>}
			*/
			editorOptions: {
				type: Array,
				value: () => []
			},
			/**
			* Type of the cell editor component to be rendered. Allowed values:
			* - `text` (default) - renders a text field
			* - `checkbox` - renders a checkbox
			* - `select` - renders a select with a list of items passed as `editorOptions`
			*
			* Editor type is set to `custom` when `editModeRenderer` is set.
			* @attr {text|checkbox|select|custom} editor-type
			*/
			editorType: {
				type: String,
				value: "text"
			},
			/**
			* Path of the property used for the value of the editor component.
			* @attr {string} editor-value-path
			*/
			editorValuePath: {
				type: String,
				value: "value"
			},
			/**
			* JS Path of the property in the item used for the editable content.
			*/
			path: {
				type: String,
				observer: "_pathChanged",
				sync: true
			},
			/**
			* A function to check whether a specific cell of this column can be
			* edited. This allows to disable editing of individual rows or cells,
			* based on the item.
			*
			* Receives a `model` object containing the item for an individual row,
			* and should return a boolean indicating whether the column's cell in
			* that row is editable.
			*
			* The `model` object contains:
			* - `model.index` The index of the item.
			* - `model.item` The item.
			* - `model.expanded` Sublevel toggle state.
			* - `model.level` Level of the tree represented with a horizontal offset of the toggle button.
			* - `model.selected` Selected state.
			*
			* @type {(model: GridItemModel) => boolean}
			*/
			isCellEditable: {
				type: Function,
				observer: "_isCellEditableChanged"
			},
			/** @private */
			_oldRenderer: Function
		};
	}
	static get observers() {
		return ["_editModeRendererChanged(editModeRenderer, __initialized)"];
	}
	constructor() {
		super();
		this._focusButtonMode = navigator.platform.includes("Mac");
		this.__editModeRenderer = function(root, column) {
			const cell = root.assignedSlot.parentNode;
			const tagName = column._getEditorTagName(cell);
			if (!root.firstElementChild || root.firstElementChild.localName.toLowerCase() !== tagName) root.innerHTML = `
          <${tagName}></${tagName}>
        `;
		};
	}
	/** @private */
	_pathChanged(path) {
		if (!path || path.length === 0) throw new Error("You should specify the path for the edit column");
	}
	/** @private */
	_isCellEditableChanged() {
		this._grid.requestContentUpdate();
	}
	/** @private */
	_editModeRendererChanged(renderer) {
		if (renderer) this.editorType = "custom";
		else if (this._oldRenderer) this.editorType = "text";
		this._oldRenderer = renderer;
	}
	/**
	* @param {!HTMLElement} cell
	* @return {string}
	* @protected
	*/
	_getEditorTagName(cell) {
		return this.editorType === "custom" ? this._getEditorComponent(cell).localName : this._getTagNameByType();
	}
	/**
	* @param {!HTMLElement} cell
	* @return {HTMLElement | null}
	* @protected
	*/
	_getEditorComponent(cell) {
		return this.editorType === "custom" ? cell._content.firstElementChild : cell._content.querySelector(this._getEditorTagName(cell));
	}
	/** @private */
	_getTagNameByType() {
		let type;
		switch (this.editorType) {
			case "checkbox":
				type = "checkbox";
				break;
			case "select":
				type = "select";
				break;
			default:
				type = "text-field";
				break;
		}
		return this.constructor.is.replace("column", type);
	}
	/** @private */
	_focusEditor(editor) {
		editor.focus();
		if (this.editorType === "checkbox") editor.setAttribute("focus-ring", "");
		else if (editor instanceof HTMLInputElement) editor.select();
		else if (editor.focusElement && editor.focusElement instanceof HTMLInputElement) editor.focusElement.select();
	}
	/**
	* @param {!HTMLElement} editor
	* @return {unknown}
	* @protected
	*/
	_getEditorValue(editor) {
		return get(this.editorType === "checkbox" ? "checked" : this.editorValuePath, editor);
	}
	/** @private */
	_renderEditor(cell, model) {
		cell.__savedRenderer = this._renderer || cell._renderer;
		cell._renderer = this.editModeRenderer || this.__editModeRenderer;
		if (cell._focusButton) cell._focusButton.removeAttribute("role");
		this._clearCellContent(cell);
		this._runRenderer(cell._renderer, cell, model);
	}
	/** @private */
	_removeEditor(cell, _model) {
		if (!cell.__savedRenderer) return;
		cell._renderer = cell.__savedRenderer;
		cell.__savedRenderer = void 0;
		this._clearCellContent(cell);
		if (cell._focusButton) cell._focusButton.setAttribute("role", "button");
		this.__renderCellsContent(cell._renderer, [cell]);
	}
	/** @private */
	_setEditorOptions(editor) {
		if (this.editorOptions && this.editorOptions.length) editor.options = this.editorOptions;
	}
	/** @private */
	_setEditorValue(editor, value) {
		const path = this.editorType === "checkbox" ? "checked" : this.editorValuePath;
		value = this.editorType === "checkbox" && typeof value === "string" ? value === "true" : value;
		set(path, value, editor);
		if (editor.notifyPath) editor.notifyPath(path, value);
	}
	/**
	* @param {!HTMLElement} cell
	* @param {!GridItemModel} model
	* @protected
	*/
	_startCellEdit(cell, model) {
		this._renderEditor(cell, model);
		const editor = this._getEditorComponent(cell);
		editor.addEventListener("focusout", this._grid.__boundEditorFocusOut);
		editor.addEventListener("focusin", this._grid.__boundEditorFocusIn);
		this._setEditorOptions(editor);
		this._setEditorValue(editor, get(this.path, model.item));
		editor._grid = this._grid;
		if (editor.updateComplete) editor.updateComplete.then(() => this._focusEditor(editor));
		else this._focusEditor(editor);
		if (this.editorType === "custom") editor.addEventListener("opened-changed", (event) => {
			const element = event.target;
			const activeElement = getDeepActiveElement();
			if (!element.opened && !element.contains(activeElement)) this._grid._stopEdit();
		});
	}
	/**
	* @param {!HTMLElement} cell
	* @param {!GridItemModel} model
	* @protected
	*/
	_stopCellEdit(cell, model) {
		this._removeEditor(cell, model);
	}
};
//#endregion
//#region node_modules/@vaadin/grid-pro/src/vaadin-grid-pro-edit-column.js
/**
* @license
* Copyright (c) 2000 - 2026 Vaadin Ltd.
*
* This program is available under Vaadin Commercial License and Service Terms.
*
* See https://vaadin.com/commercial-license-and-service-terms for the full
* license.
*/
/**
* `<vaadin-grid-pro-edit-column>` is a helper element for the `<vaadin-grid-pro>`
* that provides default inline editing for the items.
*
* __Note that the `path` property must be explicitly specified for edit column.__
*
* #### Example:
* ```html
* <vaadin-grid-pro>
*  <vaadin-grid-pro-edit-column path="name.first"></vaadin-grid-pro-edit-column>
*
*  <vaadin-grid-column>
*    ...
* ```
*
* @customElement vaadin-grid-pro-edit-column
* @extends GridColumn
*/
var GridProEditColumn = class extends GridProEditColumnMixin(GridColumn) {
	static get is() {
		return "vaadin-grid-pro-edit-column";
	}
};
defineCustomElement(GridProEditColumn);
//#endregion
export { GridProEditColumn };

//# sourceMappingURL=@vaadin_grid-pro_src_vaadin-grid-pro-edit-column__js.js.map