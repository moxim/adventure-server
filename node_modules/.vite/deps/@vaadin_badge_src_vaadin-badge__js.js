import { o as css } from "./reactive-element-CKo3LdqU.js";
import { n as html } from "./lit-html-x1Rck0rJ.js";
import { LitElement } from "./lit.js";
import { t as PolylitMixin } from "./polylit-mixin-BwU08z1T.js";
import { defineCustomElement } from "./@vaadin_component-base_src_define__js.js";
import { t as LumoInjectionMixin } from "./lumo-injection-mixin-CJJZE4Xo.js";
import { t as ThemableMixin } from "./vaadin-themable-mixin-CYKnTaeR.js";
import "./style-props-Bqqs274Y.js";
import { o as isEmptyTextNode } from "./dom-utils-DDAqRijW.js";
import { t as SlotObserver } from "./slot-observer-BXFrQCzl.js";
import { t as ElementMixin } from "./element-mixin-B8J2EQHC.js";
import { t as classMap } from "./class-map-CIDY8VVJ.js";
import { t as screenReaderOnly } from "./sr-only-styles-qAp_X7Z1.js";
//#region node_modules/@vaadin/badge/src/styles/vaadin-badge-base-styles.js
/**
* @license
* Copyright (c) 2026 - 2026 Vaadin Ltd.
* This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
*/
var badgeStyles = css`
  :host {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    vertical-align: baseline;
    box-sizing: border-box;
    gap: var(--vaadin-badge-gap, 0.25em);
    padding: var(--vaadin-badge-padding, 0 calc(0.5lh - 0.25em));
    font-size: var(--vaadin-badge-font-size, 0.875rem);
    font-family: var(--vaadin-badge-font-family, inherit);
    font-weight: var(--vaadin-badge-font-weight, 500);
    line-height: var(--vaadin-badge-line-height, 1lh);
    color: var(--vaadin-badge-text-color, var(--vaadin-text-color));
    background: var(--vaadin-badge-background, transparent);
    border: var(--vaadin-badge-border-width, 1px) solid var(--vaadin-badge-border-color, var(--vaadin-border-color));
    border-radius: var(--vaadin-badge-border-radius, 100vh);
    min-width: calc(1lh + var(--vaadin-badge-border-width, 1px) * 2);
    white-space: nowrap;
    --vaadin-icon-size: 1em;
    /* prevent from stretching */
    height: round(1lh + var(--vaadin-badge-border-width, 1px) * 2, 0.125rem);
    flex: none;
    /* Only reserve 1lh of space (treat border as an outline) */
    margin: calc(var(--vaadin-badge-border-width, 1px) * -1);
  }

  :host([hidden]) {
    display: none !important;
  }

  [part='content'] {
    overflow: hidden;
    text-overflow: ellipsis;
  }

  :host(:not([has-icon])) [part='icon'],
  :host(:not([has-content])) [part='content'],
  :host(:not([has-number])) [part='number'] {
    display: none;
  }

  :host(:is([theme~='filled'], [theme~='dot'])) {
    background: var(--vaadin-text-color);
    color: var(--vaadin-background-color);
  }

  :host([theme~='icon-only']),
  :host([has-icon]:not([has-content], [has-number])) {
    padding: 0;
  }

  :host([theme~='icon-only']),
  :host([theme~='number-only']) {
    gap: 0;
  }

  :host([theme~='dot']) {
    min-width: 0;
    width: round(0.5em, 0.125rem);
    height: round(0.5em, 0.125rem);
    padding: 0;
    margin: 0;
  }

  @media (forced-colors: active) {
    :host {
      border: 1px solid !important;
    }
  }
`;
//#endregion
//#region node_modules/@vaadin/badge/src/vaadin-badge.js
/**
* @license
* Copyright (c) 2026 - 2026 Vaadin Ltd.
* This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
*/
/**
* `<vaadin-badge>` is a Web Component for displaying badges.
*
* ```html
* <vaadin-badge>New</vaadin-badge>
* ```
*
* ### Slots
*
* Name     | Description
* ---------|-------------
* (none)   | Default slot for the badge text content
* `icon`   | Slot for an icon to place before the text
*
* ### Styling
*
* The following shadow DOM parts are available for styling:
*
* Part name  | Description
* -----------|-------------
* `icon`     | The container for the icon slot
* `number`   | The container for the number value
* `content`  | The container for the default slot
*
* The following state attributes are available for styling:
*
* Attribute      | Description
* ---------------|-------------
* `has-icon`     | Set when the badge has content in the icon slot
* `has-content`  | Set when the badge has content in the default slot
* `has-number`   | Set when the badge has a number value
*
* The following custom CSS properties are available for styling:
*
* Custom CSS property              |
* :--------------------------------|
* `--vaadin-badge-background`      |
* `--vaadin-badge-border-color`    |
* `--vaadin-badge-border-radius`   |
* `--vaadin-badge-border-width`    |
* `--vaadin-badge-font-size`       |
* `--vaadin-badge-font-weight`     |
* `--vaadin-badge-font-family`     |
* `--vaadin-badge-gap`             |
* `--vaadin-badge-line-height`     |
* `--vaadin-badge-padding`         |
* `--vaadin-badge-text-color`      |
*
* See [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation.
*
* @customElement vaadin-badge
* @extends HTMLElement
*/
var Badge = class extends ElementMixin(ThemableMixin(PolylitMixin(LumoInjectionMixin(LitElement)))) {
	static get is() {
		return "vaadin-badge";
	}
	static get styles() {
		return [badgeStyles, screenReaderOnly];
	}
	static get lumoInjector() {
		return {
			...super.lumoInjector,
			includeBaseStyles: true
		};
	}
	static get properties() {
		return { 
		/**
		* The number to display in the badge.
		*/
number: { type: Number } };
	}
	/** @protected */
	render() {
		const theme = (this._theme || "").split(" ");
		const iconOnly = theme.includes("icon-only");
		const numberOnly = theme.includes("number-only");
		const dot = theme.includes("dot");
		return html`
      <div part="icon" class="${classMap({ "sr-only": numberOnly || dot })}">
        <slot name="icon"></slot>
      </div>
      <div part="number" class="${classMap({ "sr-only": iconOnly || dot })}">${this.number}</div>
      <div part="content" class="${classMap({ "sr-only": numberOnly || iconOnly || dot })}">
        <slot></slot>
      </div>
    `;
	}
	/** @protected */
	willUpdate(props) {
		super.willUpdate(props);
		if (props.has("number")) this.toggleAttribute("has-number", this.number != null);
	}
	/** @protected */
	firstUpdated() {
		super.firstUpdated();
		const slot = this.shadowRoot.querySelector("slot:not([name])");
		this.__slotObserver = new SlotObserver(slot, ({ currentNodes }) => {
			this.toggleAttribute("has-content", currentNodes.filter((node) => !isEmptyTextNode(node)).length > 0);
		});
		const iconSlot = this.shadowRoot.querySelector("slot[name=\"icon\"]");
		this.__iconSlotObserver = new SlotObserver(iconSlot, ({ currentNodes }) => {
			this.toggleAttribute("has-icon", currentNodes.length > 0);
		});
	}
};
defineCustomElement(Badge);
//#endregion
export { Badge };

//# sourceMappingURL=@vaadin_badge_src_vaadin-badge__js.js.map