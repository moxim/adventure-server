import { t as _$LH } from "./lit-html-x1Rck0rJ.js";
//#region node_modules/lit-html/development/directive-helpers.js
/**
* @license
* Copyright 2020 Google LLC
* SPDX-License-Identifier: BSD-3-Clause
*/
var { _ChildPart: ChildPart } = _$LH;
window.ShadyDOM?.inUse && window.ShadyDOM?.noPatch === true && window.ShadyDOM.wrap;
var TemplateResultType = {
	HTML: 1,
	SVG: 2,
	MATHML: 3
};
/**
* Tests if a value is a TemplateResult or a CompiledTemplateResult.
*/
var isTemplateResult = (value, type) => type === void 0 ? value?.["_$litType$"] !== void 0 : value?.["_$litType$"] === type;
/**
* Tests whether a part has only a single-expression with no strings to
* interpolate between.
*
* Only AttributePart and PropertyPart can have multiple expressions.
* Multi-expression parts have a `strings` property and single-expression
* parts do not.
*/
var isSingleExpression = (part) => part.strings === void 0;
var RESET_VALUE = {};
/**
* Sets the committed value of a ChildPart directly without triggering the
* commit stage of the part.
*
* This is useful in cases where a directive needs to update the part such
* that the next update detects a value change or not. When value is omitted,
* the next update will be guaranteed to be detected as a change.
*
* @param part
* @param value
*/
var setCommittedValue = (part, value = RESET_VALUE) => part._$committedValue = value;
//#endregion
export { setCommittedValue as i, isSingleExpression as n, isTemplateResult as r, TemplateResultType as t };

//# sourceMappingURL=directive-helpers-DFVz-hGr.js.map