import { o as css } from "./reactive-element-CKo3LdqU.js";
import "./lit.js";
import { animationFrame } from "./@vaadin_component-base_src_async__js.js";
import { n as get, r as set } from "./polylit-mixin-BwU08z1T.js";
import { defineCustomElement } from "./@vaadin_component-base_src_define__js.js";
import "./style-props-Bqqs274Y.js";
import { Debouncer } from "./@vaadin_component-base_src_debounce__js.js";
import { t as SlotStylesMixin } from "./slot-styles-mixin-BkowbH4C.js";
import { iterateRowCells, updatePart } from "./@vaadin_grid_src_vaadin-grid-helpers__js.js";
import { n as gridStyles, t as Grid } from "./vaadin-grid-Ba7vVF3C.js";
/**
* @license
* Copyright (c) 2000 - 2026 Vaadin Ltd.
*
* This program is available under Vaadin Commercial License and Service Terms.
*
*
* See https://vaadin.com/commercial-license-and-service-terms for the full
* license.
*/
var gridProStyles = [css`
  [part~='body-cell'] {
    --_highlight-color: color-mix(in srgb, currentColor 5%, transparent);
    --_highlight-color2: color-mix(in srgb, currentColor 10%, transparent);
  }

  [part~='editable-cell'] {
    display: flex;
    align-items: inherit;
    align-self: stretch;
    flex-grow: 1;
    min-width: 0;
    cursor: var(--vaadin-clickable-cursor);
    outline: 0;
  }

  /* Show cell focus outline */
  [part~='cell']:is([part~='editable-cell']:focus, :has([part~='editable-cell']:focus))::after {
    content: '';
  }

  /* On macOS the editable-cell part is a button inside the body cell. On other platforms the body cell is the editable-cell part. */

  @media (any-hover: hover) {
    .body-cell:is([part~='editable-cell'], :has([part~='editable-cell'])):hover {
      --vaadin-grid-cell-background-color: var(--_highlight-color);
    }
  }

  :host([navigating]) .body-cell:is([part~='editable-cell']:focus, :has([part~='editable-cell']:focus)) {
    --vaadin-grid-cell-background-color: var(--_highlight-color);
  }

  /* Indicate editable cells */

  :host([theme~='highlight-editable-cells']) .body-cell:is([part~='editable-cell'], :has([part~='editable-cell'])) {
    --vaadin-grid-row-highlight-background-color: var(
      --vaadin-grid-pro-editable-cell-background-color,
      var(--_highlight-color)
    );
  }

  /* Indicate read-only cells */

  :host([theme~='highlight-read-only-cells']) .body-cell:not([part~='editable-cell'], :has([part~='editable-cell'])) {
    --_cell-highlight-background-image: repeating-linear-gradient(
      -45deg,
      transparent,
      transparent 30%,
      var(--_highlight-color2) 30%,
      var(--_highlight-color2) 50%
    );
    background-repeat: repeat;
    background-size: 6px 6px;
    background-clip: padding-box;
  }

  /* Loading editor cell styles are used by Flow GridPro */
  :host([loading-editor]) [part~='focused-cell']::before {
    content: '';
    position: absolute;
    inset: 0;
    pointer-events: none;
    box-shadow: inset 0 0 0 var(--vaadin-focus-ring-width) var(--vaadin-focus-ring-color);
    animation: loading-editor 1.4s infinite;
    opacity: 0;
  }

  @keyframes loading-editor {
    50% {
      opacity: 1;
    }
  }

  [part~='updating-cell']::after {
    content: '';
    position: absolute;
    inset: var(--_cell-padding);
    margin: var(--vaadin-focus-ring-width);
    border-radius: 4px;
    background-size: max(4em, 50%);
    background-repeat: no-repeat;
    background-position: min(-200%, -4em) 0;
    background-image: linear-gradient(90deg, transparent, currentColor, transparent);
    animation: updating-cell 1.3s ease-out infinite;
    opacity: 0.1;
  }

  @keyframes updating-cell {
    100% {
      background-position: max(300%, 8em) 0;
    }
  }

  :host([loading-editor]) [part~='focused-cell'] ::slotted(vaadin-grid-cell-content),
  [part~='updating-cell'] ::slotted(vaadin-grid-cell-content) {
    opacity: 0;
    pointer-events: none;
  }
`, gridStyles];
//#endregion
//#region node_modules/@vaadin/grid-pro/src/vaadin-grid-pro-inline-editing-mixin.js
/**
* @license
* Copyright (c) 2000 - 2026 Vaadin Ltd.
*
* This program is available under Vaadin Commercial License and Service Terms.
*
* See https://vaadin.com/commercial-license-and-service-terms for the full
* license.
*/
var InlineEditingMixin = (superClass) => class InlineEditingMixin extends superClass {
	static get properties() {
		return {
			/**
			* When true, pressing Enter while in cell edit mode
			* will move focus to the editable cell in the next row
			* (Shift + Enter - same, but for previous row).
			* @attr {boolean} enter-next-row
			*/
			enterNextRow: { type: Boolean },
			/**
			* When true, after moving to next or previous editable cell using
			* Tab / Shift+Tab, it will be focused without edit mode.
			*
			* When `enterNextRow` is true, pressing Enter will also
			* preserve edit mode, otherwise, it will have no effect.
			* @attr {boolean} single-cell-edit
			*/
			singleCellEdit: { type: Boolean },
			/**
			* When true, the grid enters cell edit mode on a single click
			* instead of the default double click.
			* @attr {boolean} edit-on-click
			*/
			editOnClick: { type: Boolean },
			/** @private */
			_editingDisabled: { type: Boolean }
		};
	}
	constructor() {
		super();
		this.__boundItemPropertyChanged = this._onItemPropertyChanged.bind(this);
		this.__boundEditorFocusOut = this._onEditorFocusOut.bind(this);
		this.__boundEditorFocusIn = this._onEditorFocusIn.bind(this);
		this._addEditColumnListener("mousedown", (e) => {
			e.stopImmediatePropagation();
			this.toggleAttribute("navigating", true);
		});
		this._addEditColumnListener("focusout", (e) => {
			e.stopImmediatePropagation();
		});
	}
	/** @protected */
	ready() {
		this.addEventListener("keydown", (e) => {
			if (this.hasAttribute("loading-editor") && ![
				"Tab",
				"Escape",
				"Enter"
			].includes(e.key)) {
				e.preventDefault();
				e.stopPropagation();
			}
		}, true);
		this.addEventListener("keydown", (e) => {
			switch (e.keyCode) {
				case 27:
					if (this.__edited) this._stopEdit(true);
					break;
				case 9:
					if (this.__edited) this._switchEditCell(e);
					break;
				case 13:
					if (this.__edited) this._switchEditCell(e);
					else this._enterEditFromEvent(e);
					break;
				case 32:
					if (!this.__edited) this._enterEditFromEvent(e);
					break;
				default:
					if (e.key && e.key.length === 1) this._enterEditFromEvent(e, "text");
					break;
			}
		});
		super.ready();
		this.$.table.addEventListener("click", (e) => {
			const column = this.getEventContext(e).column;
			if (column && this._isEditColumn(column)) {
				if (this.editOnClick) this._enterEditFromEvent(e);
			}
		});
		if (this._ios) {
			let firstClickTime;
			let waitingSecondClick = false;
			this.addEventListener("click", (e) => {
				if (!waitingSecondClick) {
					firstClickTime = (/* @__PURE__ */ new Date()).getTime();
					waitingSecondClick = true;
					setTimeout(() => {
						waitingSecondClick = false;
					}, 300);
				} else {
					waitingSecondClick = false;
					if ((/* @__PURE__ */ new Date()).getTime() - firstClickTime < 300) this._enterEditFromEvent(e);
				}
			});
		} else this.addEventListener("dblclick", (e) => {
			if (!this.editOnClick) this._enterEditFromEvent(e);
		});
	}
	/**
	* Prevents making an edit column cell's parent row active on click.
	*
	* @override
	* @protected
	*/
	_shouldPreventCellActivationOnClick(e) {
		return super._shouldPreventCellActivationOnClick(e) || this._isEditColumn(this.getEventContext(e).column);
	}
	/**
	* Override an observer from `DisabledMixin` to stop
	* editing when grid element becomes disabled.
	*
	* @param {boolean} disabled
	* @param {boolean} oldDisabled
	* @protected
	* @override
	*/
	_disabledChanged(disabled, oldDisabled) {
		super._disabledChanged(disabled, oldDisabled);
		if (disabled && this.__edited) this._stopEdit(true);
	}
	/** @protected */
	_checkImports() {
		super._checkImports();
		["vaadin-grid-pro-edit-column"].forEach((elementName) => {
			if (this.querySelector(elementName) && !customElements.get(elementName)) console.warn(`Make sure you have imported the required module for <${elementName}> element.`);
		});
	}
	/** @private */
	_applyEdit({ path, value, item }) {
		set(path, value, item);
		this.requestContentUpdate();
	}
	/** @private */
	_addEditColumnListener(type, callback) {
		this.addEventListener(type, (e) => {
			const context = this.getEventContext(e);
			const column = context.column;
			const edited = this.__edited;
			if (context.item && this._isEditColumn(column)) {
				const { cell } = this._getGridEventLocation(e);
				if (!cell || cell.getAttribute("part").indexOf("details-cell") > -1) return;
				if (edited?.cell === cell) return;
				if (!this._isCellEditable(cell)) return;
				callback(e);
			}
		});
	}
	/** @private */
	_onItemPropertyChanged(e) {
		if (!e.defaultPrevented) this._applyEdit(e.detail);
	}
	/** @private */
	_getRowByIndex(index) {
		return Array.from(this.$.items.children).find((el) => el.index === index);
	}
	/** @private */
	_isEditColumn(column) {
		return column?.localName.toLowerCase() === "vaadin-grid-pro-edit-column";
	}
	/** @private */
	_getEditColumns() {
		const columnTreeLevel = this._columnTree.length - 1;
		return this._columnTree[columnTreeLevel].filter((column) => this._isEditColumn(column) && !column.hidden).sort((a, b) => a._order - b._order);
	}
	/** @private */
	_cancelStopEdit() {
		if (this._debouncerStopEdit) {
			this._debouncerStopEdit.cancel();
			delete this._debouncerStopEdit;
		}
	}
	/** @private */
	_flushStopEdit() {
		if (this._debouncerStopEdit) {
			this._debouncerStopEdit.flush();
			delete this._debouncerStopEdit;
		}
	}
	/** @private */
	_enterEditFromEvent(e, type) {
		const context = this.getEventContext(e);
		const column = context.column;
		const edited = this.__edited;
		if (context.item && this._isEditColumn(column)) {
			const { cell } = this._getGridEventLocation(e);
			if (!cell || cell.getAttribute("part").indexOf("details-cell") > -1) return;
			if (type && column.editorType !== type) return;
			if (edited?.cell === cell && column._getEditorComponent(cell).contains(e.target)) return;
			this._flushStopEdit();
			this._startEdit(cell, column);
		} else if (edited) this._stopEdit();
	}
	/** @private */
	_onEditorFocusOut(event) {
		if (this.__shouldIgnoreFocusOut(event)) return;
		this._debouncerStopEdit = Debouncer.debounce(this._debouncerStopEdit, animationFrame, this._stopEdit.bind(this));
	}
	/**
	* Override method from ScrollMixin to stop editing if the edited cell
	* is scrolled out of the view and removed from the DOM.
	* @private
	*/
	__updateColumnsBodyContentHidden() {
		super.__updateColumnsBodyContentHidden();
		if (this.__edited && !this.__edited.cell.isConnected) this._stopEdit(true, false);
	}
	/** @private */
	__shouldIgnoreFocusOut(event) {
		if (this.__edited) {
			const { cell, column } = this.__edited;
			const editor = column._getEditorComponent(cell);
			const path = event.composedPath();
			if (path.slice(0, path.indexOf(editor) + 1).filter((node) => node.nodeType === Node.ELEMENT_NODE).some((el) => typeof el._shouldRemoveFocus === "function" && !el._shouldRemoveFocus(event))) return true;
		}
	}
	/** @private */
	_onEditorFocusIn() {
		this._cancelStopEdit();
	}
	/** @private */
	_startEdit(cell, column) {
		const isCellEditable = this._isCellEditable(cell);
		if (this.disabled || this._editingDisabled || !isCellEditable) return;
		this._cancelStopEdit();
		this.scrollToColumn(column);
		const model = this.__getRowModel(cell.__parentRow);
		this.__edited = {
			cell,
			column,
			model
		};
		column._startCellEdit(cell, model);
		this.dispatchEvent(new CustomEvent("cell-edit-started", {
			detail: {
				index: model.index,
				item: model.item,
				path: column.path
			},
			composed: true
		}));
		this.addEventListener("item-property-changed", this.__boundItemPropertyChanged);
	}
	/**
	* @param {boolean=} shouldCancel
	* @param {boolean=} shouldRestoreFocus
	* @protected
	*/
	_stopEdit(shouldCancel, shouldRestoreFocus) {
		if (!this.__edited) return;
		const { cell, column, model } = this.__edited;
		if (!shouldCancel && !this.hasAttribute("loading-editor")) {
			const editor = column._getEditorComponent(cell);
			if (editor) {
				const value = column._getEditorValue(editor);
				if (value !== get(column.path, model.item)) {
					editor.blur();
					this.dispatchEvent(new CustomEvent("item-property-changed", {
						detail: {
							index: model.index,
							item: model.item,
							path: column.path,
							value
						},
						bubbles: true,
						cancelable: true,
						composed: true
					}));
				}
			}
		}
		column._stopCellEdit(cell, model);
		this.__edited = null;
		this.removeEventListener("item-property-changed", this.__boundItemPropertyChanged);
		if (shouldRestoreFocus) cell.focus();
	}
	/**
	* @param {!KeyboardEvent} e
	* @protected
	*/
	async _switchEditCell(e) {
		if (e.defaultPrevented && e.keyCode === 9) return;
		this._cancelStopEdit();
		const editableColumns = this._getEditColumns();
		const { cell, column, model } = this.__edited;
		e.stopImmediatePropagation();
		const editor = column._getEditorComponent(cell);
		if (e.key === "Tab" && editor?.contains(e.target)) {
			if (await new Promise((resolve) => {
				editor.addEventListener("focusout", (event) => {
					resolve(this.__shouldIgnoreFocusOut(event));
				}, { once: true });
			})) return;
		} else e.preventDefault();
		this._stopEdit();
		let nextIndex = model.index;
		let nextColumn = column;
		let nextCell = cell;
		let directionX = 0;
		let directionY = 0;
		if (e.keyCode === 13 && this.enterNextRow) directionY = e.shiftKey ? -1 : 1;
		if (e.keyCode === 9) directionX = e.shiftKey ? -1 : 1;
		if (directionX || directionY) while (nextCell) {
			if (directionX) {
				nextColumn = editableColumns[editableColumns.indexOf(nextColumn) + directionX];
				if (!nextColumn) {
					nextIndex += directionX;
					nextColumn = editableColumns[directionX > 0 ? 0 : editableColumns.length - 1];
				}
			}
			if (directionY) nextIndex += directionY;
			const nextRow = this._getRowByIndex(nextIndex);
			nextCell = nextRow && Array.from(nextRow.__cells).find((cell) => cell._column === nextColumn);
			if (nextCell && this._isCellEditable(nextCell)) break;
		}
		if (!nextCell) {
			nextCell = cell;
			nextIndex = model.index;
		}
		if (!this.singleCellEdit && nextCell !== cell) this._startEdit(nextCell, nextColumn);
		else {
			this.__ensureFlatIndexInViewport(nextIndex);
			nextCell.focus();
		}
	}
	/**
	* @param {!HTMLElement} row
	* @private
	*/
	__updateRow(row) {
		const item = this.__getRowItem(row);
		if (this.__edited) {
			const { cell, model } = this.__edited;
			if (!this._isCellEditable(cell)) this._stopEdit(true, true);
			else if (cell.__parentRow === row && item && this.getItemId(model.item) !== this.getItemId(item)) this._stopEdit();
		}
		super.__updateRow(row, item);
	}
	/**
	* Override method from `StylingMixin` to apply `editable-cell` part to the
	* cells of edit columns.
	*
	* @override
	*/
	_generateCellPartNames(row, model) {
		super._generateCellPartNames(row, model);
		iterateRowCells(row, (cell) => {
			const isEditable = !row.hasAttribute("loading") && this._isCellEditable(cell);
			updatePart(cell._focusButton || cell, "editable-cell", isEditable);
		});
	}
	/** @private */
	_isCellEditable(cell) {
		const column = cell._column;
		if (!this._isEditColumn(column)) return false;
		if (!column.isCellEditable) return true;
		const model = this.__getRowModel(cell.__parentRow);
		return column.isCellEditable(model);
	}
};
//#endregion
//#region node_modules/@vaadin/grid-pro/src/vaadin-grid-pro.js
/**
* @license
* Copyright (c) 2000 - 2026 Vaadin Ltd.
*
* This program is available under Vaadin Commercial License and Service Terms.
*
* See https://vaadin.com/commercial-license-and-service-terms for the full
* license.
*/
/**
* `<vaadin-grid-pro>` is a high quality data grid / data table Web Component with extended functionality.
* It extends `<vaadin-grid>` and adds extra features on top of the basic ones.
*
* See [`<vaadin-grid>`](#/elements/vaadin-grid) documentation for details.
*
* ```html
* <vaadin-grid-pro></vaadin-grid-pro>
* ```
*
* ### Internal components
*
* In addition to `<vaadin-grid-pro>` itself, the following internal
* components are themable:
*
* - `<vaadin-grid-pro-edit-checkbox>` - has the same API as [`<vaadin-checkbox>`](#/elements/vaadin-checkbox).
* - `<vaadin-grid-pro-edit-text-field>` - has the same API as [`<vaadin-text-field>`](#/elements/vaadin-text-field).
* - `<vaadin-grid-pro-edit-select>` - has the same API as [`<vaadin-select>`](#/elements/vaadin-select).
*
* @fires {CustomEvent} active-item-changed - Fired when the `activeItem` property changes.
* @fires {CustomEvent} cell-activate - Fired when the cell is activated with click or keyboard.
* @fires {CustomEvent} cell-edit-started - Fired when the user starts editing a grid cell.
* @fires {CustomEvent} column-reorder - Fired when the columns in the grid are reordered.
* @fires {CustomEvent} column-resize - Fired when the grid column resize is finished.
* @fires {CustomEvent} expanded-items-changed - Fired when the `expandedItems` property changes.
* @fires {CustomEvent} grid-dragstart - Fired when starting to drag grid rows.
* @fires {CustomEvent} grid-dragend - Fired when the dragging of the rows ends.
* @fires {CustomEvent} grid-drop - Fired when a drop occurs on top of the grid.
* @fires {CustomEvent} item-property-changed - Fired before exiting cell edit mode if the value has been changed. If the default is prevented, the value change is not applied.
* @fires {CustomEvent} loading-changed - Fired when the `loading` property changes.
* @fires {CustomEvent} selected-items-changed - Fired when the `selectedItems` property changes.
*
* @customElement vaadin-grid-pro
* @extends Grid
*/
var GridPro = class extends SlotStylesMixin(InlineEditingMixin(Grid)) {
	static get is() {
		return "vaadin-grid-pro";
	}
	static get cvdlName() {
		return "vaadin-grid-pro";
	}
	static get styles() {
		return gridProStyles;
	}
	get slotStyles() {
		const tag = this.localName;
		return [`
      ${tag} [theme="grid-pro-editor"] {
        --vaadin-input-field-border-radius: 0px;
        --vaadin-input-field-border-width: 0px;
        width: 100%;
      }

      ${tag} [theme="grid-pro-editor"]::part(input-field) {
        height: 100%;
        outline-offset: calc(var(--vaadin-focus-ring-width) * -1);
      }

      vaadin-grid-cell-content:has([theme="grid-pro-editor"]) {
        padding: 0;
        overflow: visible;
      }
    `];
	}
};
defineCustomElement(GridPro);
//#endregion
export { GridPro };

//# sourceMappingURL=@vaadin_grid-pro_src_vaadin-grid-pro__js.js.map