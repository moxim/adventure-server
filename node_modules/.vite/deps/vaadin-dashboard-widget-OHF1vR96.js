import { o as css } from "./reactive-element-CKo3LdqU.js";
import { n as html } from "./lit-html-x1Rck0rJ.js";
import { LitElement } from "./lit.js";
import { t as PolylitMixin } from "./polylit-mixin-BwU08z1T.js";
import { defineCustomElement } from "./@vaadin_component-base_src_define__js.js";
import { t as LumoInjectionMixin } from "./lumo-injection-mixin-CJJZE4Xo.js";
import { t as ThemableMixin } from "./vaadin-themable-mixin-CYKnTaeR.js";
import "./style-props-Bqqs274Y.js";
import { t as ElementMixin } from "./element-mixin-B8J2EQHC.js";
import { a as WRAPPER_LOCAL_NAME, f as widgetFixedHeightDeclarations, i as SYNCHRONIZED_ATTRIBUTES, n as DashboardItemMixin, o as findAncestorInstance, r as getDefaultI18n, t as DashboardSection, u as dashboardWidgetAndSectionStyles } from "./vaadin-dashboard-section-D1MASVP6.js";
/**
* @license
* Copyright (c) 2000 - 2026 Vaadin Ltd.
*
* This program is available under Vaadin Commercial License and Service Terms.
*
*
* See https://vaadin.com/commercial-license-and-service-terms for the full
* license.
*/
var dashboardWidgetStyles = [css`
  :host {
    display: flex;
    flex-direction: column;
    grid-column: var(--_item-column);
    grid-row: var(--_item-row);
    position: relative;
    background: var(--_widget-background);
    border-radius: var(--_widget-border-radius);
    box-shadow: var(--_widget-shadow);
    border: var(--_widget-border-width) solid var(--_widget-border-color);

    /* Pin to the fixed row height when one is set. The widget host's own definition is needed so widgets nested inside sections in vaadin-dashboard-layout (where the layout-level cap doesn't reach) still get capped. */
    ${widgetFixedHeightDeclarations};
  }

  :host([hidden]) {
    display: none !important;
  }

  :host(:not([editable])) [part~='resize-button'] {
    display: none;
  }

  [part~='content'] {
    flex: 1;
    overflow: hidden;
    min-height: 1em;
    border-bottom-left-radius: inherit;
    border-bottom-right-radius: inherit;
  }

  [part~='resize-button'] {
    position: absolute;
    bottom: 0;
    inset-inline-end: 0;
    z-index: 1;
    overflow: hidden;
    touch-action: none;
    cursor: nwse-resize;
    --icon: var(--_vaadin-icon-resize);
  }

  :host([dir='rtl']) [part~='resize-button'] {
    cursor: sw-resize;
  }

  :host([dir='rtl']) [part~='resize-button'] .icon::before {
    transform: scaleX(-1);
  }

  /* Widget states */

  :host([editable]) {
    --vaadin-dashboard-widget-shadow: var(--_widget-editable-shadow);
  }

  :host([selected]) {
    --vaadin-dashboard-widget-shadow: var(--_widget-selected-shadow);
  }

  :host([resizing])::after {
    content: '';
    z-index: 2;
    position: absolute;
    top: 0;
    width: var(--_widget-resizer-width, 0);
    height: var(--_widget-resizer-height, 0);
    background: color-mix(in srgb, currentColor 5%, var(--_widget-background) 70%);
    border-color: color-mix(in srgb, currentColor 10%, transparent);
    border-width: var(--_widget-border-width);
    border-style: solid;
    border-radius: inherit;
  }

  /* Widget parts */
  header {
    padding: var(
      --vaadin-dashboard-widget-header-padding,
      var(--vaadin-padding-block-container) var(--vaadin-padding-inline-container)
    );
  }

  :host([editable]) header {
    padding: var(--vaadin-dashboard-widget-header-padding, var(--vaadin-padding-s));
  }

  header:has([part~='title'][hidden]) {
    padding: 0;
  }
`, dashboardWidgetAndSectionStyles];
//#endregion
//#region node_modules/@vaadin/dashboard/src/vaadin-dashboard-widget.js
/**
* @license
* Copyright (c) 2000 - 2026 Vaadin Ltd.
*
* This program is available under Vaadin Commercial License and Service Terms.
*
* See https://vaadin.com/commercial-license-and-service-terms for the full
* license.
*/
/**
* A Widget component for use with the Dashboard component
*
* ```html
* <vaadin-dashboard-widget widget-title="Title">
*   <span slot="header-content">Header</span>
*   <div>Content</div>
* </vaadin-dashboard-widget>
* ```
*
* ### Customization
*
* You can configure the item by using `slot` names.
*
* Slot name        | Description
* -----------------|-------------
* `header-content` | A slot for the widget header content.
*
* #### Example
*
* ```html
* <vaadin-dashboard-widget widget-title="Title">
*   <span slot="header-content">Header</span>
*   <div>Content</div>
* </vaadin-dashboard-widget>
* ```
*
* ### Styling
*
* The following shadow DOM parts are available for styling:
*
* Part name                     | Description
* ------------------------------|-------------
* `header`                      | The header of the widget
* `title`                       | The title of the widget
* `content`                     | The content of the widget
* `button`                      | Set on all the buttons (move, resize and others)
* `move-button`                 | The move button
* `remove-button`               | The remove button
* `resize-button`               | The resize button
* `move-backward-button`        | The move backward button when in move mode
* `move-forward-button`         | The move forward button when in move mode
* `move-apply-button`           | The apply button when in move mode
* `resize-shrink-width-button`  | The shrink width button when in resize mode
* `resize-grow-width-button`    | The grow width button when in resize mode
* `resize-shrink-height-button` | The shrink height button when in resize mode
* `resize-grow-height-button`   | The grow height button when in resize mode
* `resize-apply-button`         | The apply button when in resize mode
*
* The following custom properties are available:
*
* Custom Property                             | Description
* --------------------------------------------|----------------------
* `--vaadin-dashboard-widget-colspan`         | colspan of the widget
* `--vaadin-dashboard-widget-rowspan`         | rowspan of the widget
* `--vaadin-dashboard-widget-background`      | widget background
* `--vaadin-dashboard-widget-border-radius`   | widget corner radius
* `--vaadin-dashboard-widget-border-width`    | widget border width
* `--vaadin-dashboard-widget-border-color`    | widget border color
* `--vaadin-dashboard-widget-shadow`          | widget shadow (non edit mode)
* `--vaadin-dashboard-widget-padding`         | padding around widget content
* `--vaadin-dashboard-widget-title-wrap`      | widget title wrap
*
* The following state attributes are available for styling:
*
* Attribute      | Description
* ---------------|-------------
* `selected`     | Set when the element is selected.
* `focused`      | Set when the element is focused.
* `move-mode`    | Set when the element is being moved.
* `resize-mode`  | Set when the element is being resized.
* `resizing`     | Set when the element is being resized.
* `dragging`     | Set when the element is being dragged.
* `editable`     | Set when the element is editable.
* `first-child`  | Set when the element is the first child of the parent.
* `last-child`   | Set when the element is the last child of the parent.
*
* See [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation.
*
* @customElement vaadin-dashboard-widget
* @extends HTMLElement
*/
var DashboardWidget = class extends DashboardItemMixin(ElementMixin(ThemableMixin(PolylitMixin(LumoInjectionMixin(LitElement))))) {
	static get is() {
		return "vaadin-dashboard-widget";
	}
	static get styles() {
		return dashboardWidgetStyles;
	}
	static get lumoInjector() {
		return {
			...super.lumoInjector,
			includeBaseStyles: true
		};
	}
	static get properties() {
		return {
			/**
			* The object used to localize this component.
			*
			* To change the default localization, replace the entire
			* `i18n` object with a custom one.
			*
			* The object has the following structure and default values:
			* ```js
			* {
			*   selectWidget: 'Select widget for editing',
			*   remove: 'Remove',
			*   resize: 'Resize',
			*   resizeApply: 'Apply',
			*   resizeShrinkWidth: 'Shrink width',
			*   resizeGrowWidth: 'Grow width',
			*   resizeShrinkHeight: 'Shrink height',
			*   resizeGrowHeight: 'Grow height',
			*   move: 'Move',
			*   moveApply: 'Apply',
			*   moveForward: 'Move Forward',
			*   moveBackward: 'Move Backward',
			* }
			* ```
			* @private
			*/
			__i18n: {
				type: Object,
				value: () => {
					return getDefaultI18n();
				}
			},
			/**
			* The title of the widget.
			*
			* @attr {string} widget-title
			*/
			widgetTitle: {
				type: String,
				value: ""
			},
			__isNestedWidget: {
				type: Boolean,
				value: false
			}
		};
	}
	/** @protected */
	render() {
		return html`
      ${this.__renderMoveControls()} ${this.__renderResizeControls()}

      <div id="focustrap">
        ${this.__renderFocusButton("selectWidget")}

        <header part="header">
          ${this.__renderDragHandle()} ${this.__renderWidgetTitle()}
          <slot name="header-content"></slot>
          ${this.__renderRemoveButton()}
        </header>

        ${this.__renderResizeHandle()}
      </div>

      <div id="content" part="content">
        <slot></slot>
      </div>
    `;
	}
	/** @protected */
	connectedCallback() {
		super.connectedCallback();
		const wrapper = this.closest(WRAPPER_LOCAL_NAME);
		if (wrapper) {
			SYNCHRONIZED_ATTRIBUTES.forEach((attr) => {
				this.toggleAttribute(attr, !!wrapper[attr]);
			});
			this.__i18n = wrapper.i18n;
		}
		this.__updateNestedState();
		const undefinedAncestor = this.closest("*:not(:defined)");
		if (undefinedAncestor) customElements.whenDefined(undefinedAncestor.localName).then(() => {
			queueMicrotask(() => this.__updateNestedState());
		});
	}
	/** @protected */
	ready() {
		super.ready();
		if (!this.hasAttribute("role")) this.setAttribute("role", "article");
	}
	/** @private */
	__renderWidgetTitle() {
		let effectiveHeadingLevel = this._rootHeadingLevel;
		if (effectiveHeadingLevel == null) effectiveHeadingLevel = 2;
		if (this.__isNestedWidget) effectiveHeadingLevel += 1;
		return html`<div
      id="title"
      part="title"
      role="heading"
      aria-level=${effectiveHeadingLevel}
      .hidden=${!this.widgetTitle}
      >${this.widgetTitle}</div
    >`;
	}
	/** @private */
	__updateNestedState() {
		this.__isNestedWidget = !!findAncestorInstance(this, DashboardSection);
	}
};
defineCustomElement(DashboardWidget);
//#endregion
export { DashboardWidget as t };

//# sourceMappingURL=vaadin-dashboard-widget-OHF1vR96.js.map