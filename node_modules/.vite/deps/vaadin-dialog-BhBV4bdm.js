import { o as css } from "./reactive-element-CKo3LdqU.js";
import { n as html } from "./lit-html-x1Rck0rJ.js";
import { LitElement } from "./lit.js";
import { t as PolylitMixin } from "./polylit-mixin-BwU08z1T.js";
import { defineCustomElement } from "./@vaadin_component-base_src_define__js.js";
import { t as DirMixin } from "./dir-mixin-rxMTFGYq.js";
import { t as LumoInjectionMixin } from "./lumo-injection-mixin-CJJZE4Xo.js";
import { n as ThemePropertyMixin, t as ThemableMixin } from "./vaadin-themable-mixin-CYKnTaeR.js";
import { t as SlotObserver } from "./slot-observer-BXFrQCzl.js";
import { t as ElementMixin } from "./element-mixin-B8J2EQHC.js";
import { isTouch } from "./@vaadin_component-base_src_browser-utils__js.js";
import { r as setOverlayStateAttribute, t as OverlayMixin } from "./vaadin-overlay-mixin-Cp6vfu5u.js";
import { t as ifDefined } from "./if-defined-HGTsiUgW.js";
import { r as dialogOverlayStyles, t as DialogSizeMixin } from "./vaadin-dialog-size-mixin-C5bt7ZJI.js";
import { n as DialogOverflowController, t as DialogBaseMixin } from "./vaadin-dialog-base-mixin-Bbc7Kx88.js";
//#region node_modules/@vaadin/dialog/src/vaadin-dialog-overlay-mixin.js
/**
* @license
* Copyright (c) 2017 - 2026 Vaadin Ltd.
* This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
*/
var DialogOverlayMixin = (superClass) => class DialogOverlayMixin extends OverlayMixin(superClass) {
	static get properties() {
		return {
			/**
			* String used for rendering a dialog title.
			*/
			headerTitle: { type: String },
			/**
			* Custom function for rendering the dialog header.
			*/
			headerRenderer: { type: Object },
			/**
			* Custom function for rendering the dialog footer.
			*/
			footerRenderer: { type: Object },
			/**
			* Whether to keep the overlay within the viewport.
			*/
			keepInViewport: {
				type: Boolean,
				reflectToAttribute: true
			}
		};
	}
	static get observers() {
		return ["_headerFooterRendererChange(headerRenderer, footerRenderer, opened)", "_headerTitleChanged(headerTitle, opened)"];
	}
	/**
	* Override method from OverlayFocusMixin to use owner as content root
	* @protected
	* @override
	*/
	get _contentRoot() {
		return this.owner;
	}
	/**
	* Override method from OverlayMixin to use slotted div as a renderer root.
	* @protected
	* @override
	*/
	get _rendererRoot() {
		if (!this.__savedRoot) {
			const root = document.createElement("vaadin-dialog-content");
			root.style.display = "contents";
			this.owner.appendChild(root);
			this.__savedRoot = root;
		}
		return this.__savedRoot;
	}
	/** @protected */
	ready() {
		super.ready();
		this.__overflowController = new DialogOverflowController(this);
		this.addController(this.__overflowController);
		this.__resizeObserver = new ResizeObserver(() => {
			requestAnimationFrame(() => {
				this.__adjustPosition();
			});
		});
		this.__resizeObserver.observe(this.$.resizerContainer);
		const headerSlot = this.shadowRoot.querySelector("slot[name=\"header-content\"]");
		this.__headerSlotObserver = new SlotObserver(headerSlot, ({ currentNodes }) => {
			setOverlayStateAttribute(this, "has-header", currentNodes.length > 0);
			this.__overflowController.update();
		});
		const footerSlot = this.shadowRoot.querySelector("slot[name=\"footer\"]");
		this.__footerSlotObserver = new SlotObserver(footerSlot, ({ currentNodes }) => {
			setOverlayStateAttribute(this, "has-footer", currentNodes.length > 0);
			this.__overflowController.update();
		});
		this.__handleWindowResize = this.__handleWindowResize.bind(this);
	}
	updated(props) {
		super.updated(props);
		if (props.has("opened") || props.has("keepInViewport")) if (this.opened && this.keepInViewport) window.addEventListener("resize", this.__handleWindowResize);
		else window.removeEventListener("resize", this.__handleWindowResize);
	}
	/** @private */
	__createContainer(slot) {
		const container = document.createElement("vaadin-dialog-content");
		container.setAttribute("slot", slot);
		return container;
	}
	/** @private */
	__clearContainer(container) {
		container.innerHTML = "";
		delete container._$litPart$;
	}
	/** @private */
	__initContainer(container, slot) {
		if (container) this.__clearContainer(container);
		else {
			container = this.__createContainer(slot);
			this.owner.appendChild(container);
		}
		return container;
	}
	/** @private */
	_headerFooterRendererChange(headerRenderer, footerRenderer, opened) {
		const headerRendererChanged = this.__oldHeaderRenderer !== headerRenderer;
		this.__oldHeaderRenderer = headerRenderer;
		const footerRendererChanged = this.__oldFooterRenderer !== footerRenderer;
		this.__oldFooterRenderer = footerRenderer;
		const openedChanged = this._oldOpenedFooterHeader !== opened;
		this._oldOpenedFooterHeader = opened;
		if (headerRendererChanged) {
			if (headerRenderer) this.headerContainer = this.__initContainer(this.headerContainer, "header-content");
			else if (this.headerContainer) {
				this.headerContainer.remove();
				this.headerContainer = null;
			}
		}
		if (footerRendererChanged) {
			if (footerRenderer) this.footerContainer = this.__initContainer(this.footerContainer, "footer");
			else if (this.footerContainer) {
				this.footerContainer.remove();
				this.footerContainer = null;
			}
		}
		if (headerRenderer && (headerRendererChanged || openedChanged) || footerRenderer && (footerRendererChanged || openedChanged)) {
			if (opened) this.requestContentUpdate();
		}
	}
	/** @private */
	_headerTitleChanged(headerTitle, opened) {
		setOverlayStateAttribute(this, "has-title", !!headerTitle);
		if (opened && (headerTitle || this._oldHeaderTitle)) this.requestContentUpdate();
		this._oldHeaderTitle = headerTitle;
	}
	/** @private */
	_headerTitleRenderer() {
		if (this.headerTitle) {
			if (!this.headerTitleElement) {
				this.headerTitleElement = document.createElement("h2");
				this.headerTitleElement.setAttribute("slot", "title");
				this.headerTitleElement.classList.add("draggable");
			}
			this.owner.appendChild(this.headerTitleElement);
			this.headerTitleElement.textContent = this.headerTitle;
		} else if (this.headerTitleElement) {
			this.headerTitleElement.remove();
			this.headerTitleElement = null;
		}
	}
	/**
	* @override
	*/
	requestContentUpdate() {
		super.requestContentUpdate();
		if (this.headerContainer && this.headerRenderer) this.headerRenderer.call(this.owner, this.headerContainer, this.owner);
		if (this.footerContainer && this.footerRenderer) this.footerRenderer.call(this.owner, this.footerContainer, this.owner);
		this._headerTitleRenderer();
		this.__overflowController?.update();
	}
	/**
	* Retrieves the coordinates of the overlay.
	* @return {!DialogOverlayBounds}
	*/
	getBounds() {
		const overlayBounds = this.$.overlay.getBoundingClientRect();
		const containerBounds = this.getBoundingClientRect();
		return {
			top: overlayBounds.top - containerBounds.top,
			left: overlayBounds.left - containerBounds.left,
			width: overlayBounds.width,
			height: overlayBounds.height
		};
	}
	/**
	* Override method from OverlayMixin to adjust the position of the overlay if `keepInViewport` is true.
	* @override
	*/
	setBounds(bounds, absolute = true) {
		super.setBounds(bounds, absolute);
		this.__adjustPosition();
	}
	/** @private */
	__handleWindowResize() {
		this.__adjustPosition();
	}
	/**
	* Adjusts the position of the overlay to keep it within the viewport if `keepInViewport` is true.
	* @private
	*/
	__adjustPosition() {
		if (!this.opened || !this.keepInViewport) return;
		const style = getComputedStyle(this.$.overlay);
		if (style.position !== "absolute") return;
		const overlayHostBounds = this.getBoundingClientRect();
		const bounds = this.getBounds();
		const width = parseFloat(style.width) || bounds.width;
		const height = parseFloat(style.height) || bounds.height;
		const maxLeft = overlayHostBounds.right - overlayHostBounds.left - width;
		const maxTop = overlayHostBounds.bottom - overlayHostBounds.top - height;
		if (bounds.left > maxLeft || bounds.top > maxTop) {
			const left = Math.max(0, Math.min(bounds.left, maxLeft));
			const top = Math.max(0, Math.min(bounds.top, maxTop));
			Object.assign(this.$.overlay.style, {
				left: `${left}px`,
				top: `${top}px`
			});
		}
	}
};
//#endregion
//#region node_modules/@vaadin/dialog/src/vaadin-dialog-overlay.js
/**
* @license
* Copyright (c) 2017 - 2026 Vaadin Ltd.
* This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
*/
/**
* An element used internally by `<vaadin-dialog>`. Not intended to be used separately.
*
* @customElement vaadin-dialog-overlay
* @extends HTMLElement
* @private
*/
var DialogOverlay = class extends DialogOverlayMixin(DirMixin(ThemableMixin(PolylitMixin(LumoInjectionMixin(LitElement))))) {
	static get is() {
		return "vaadin-dialog-overlay";
	}
	static get styles() {
		return dialogOverlayStyles;
	}
	/**
	* Override method from OverlayFocusMixin to use owner as focus trap root
	* @protected
	* @override
	*/
	get _focusTrapRoot() {
		return this.owner;
	}
	/** @protected */
	render() {
		return html`
      <div id="backdrop" part="backdrop" ?hidden="${!this.withBackdrop}"></div>
      <div part="overlay" id="overlay">
        <section id="resizerContainer" class="resizer-container">
          <header part="header">
            <div part="title"><slot name="title"></slot></div>
            <div part="header-content"><slot name="header-content"></slot></div>
          </header>
          <div part="content" id="content"><slot></slot></div>
          <footer part="footer"><slot name="footer"></slot></footer>
        </section>
      </div>
    `;
	}
};
defineCustomElement(DialogOverlay);
//#endregion
//#region node_modules/@vaadin/dialog/src/vaadin-dialog-utils.js
/**
* @license
* Copyright (c) 2017 - 2026 Vaadin Ltd.
* This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
*/
/**
* Checks if the argument is a touch event and if so, returns a first touch.
* Otherwise, if the mouse event was passed, returns it as is.
* @param {!MouseEvent | !TouchEvent} e
* @return {!MouseEvent | !Touch}
* @protected
*/
function getMouseOrFirstTouchEvent(e) {
	return e.touches ? e.touches[0] : e;
}
/**
* Checks whether a mouse or touch event is in window.
* @param {!MouseEvent | !TouchEvent} e
* @return {boolean}
* @protected
*/
function eventInWindow(e) {
	return e.clientX >= 0 && e.clientX <= window.innerWidth && e.clientY >= 0 && e.clientY <= window.innerHeight;
}
//#endregion
//#region node_modules/@vaadin/dialog/src/vaadin-dialog-draggable-mixin.js
/**
* @license
* Copyright (c) 2017 - 2026 Vaadin Ltd.
* This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
*/
var DialogDraggableMixin = (superClass) => class VaadinDialogDraggableMixin extends superClass {
	static get properties() {
		return {
			/**
			* Set to true to enable repositioning the dialog by clicking and dragging.
			*
			* By default, only the overlay area can be used to drag the element. But,
			* a child element can be marked as a draggable area by adding a
			* "`draggable`" class to it, this will by default make all of its children draggable also.
			* If you want a child element to be draggable
			* but still have its children non-draggable (by default), mark it with
			* "`draggable-leaf-only`" class name.
			*/
			draggable: {
				type: Boolean,
				value: false,
				reflectToAttribute: true
			},
			/** @private */
			_touchDevice: {
				type: Boolean,
				value: isTouch
			},
			__dragHandleClassName: { type: String }
		};
	}
	/** @protected */
	ready() {
		super.ready();
		this._originalBounds = {};
		this._originalMouseCoords = {};
		this._startDrag = this._startDrag.bind(this);
		this._drag = this._drag.bind(this);
		this._stopDrag = this._stopDrag.bind(this);
		this.$.overlay.$.overlay.addEventListener("mousedown", this._startDrag);
		this.$.overlay.$.overlay.addEventListener("touchstart", this._startDrag);
	}
	/** @private */
	_startDrag(e) {
		if (e.type === "touchstart" && e.touches.length > 1) return;
		if (e.defaultPrevented) return;
		if (this.draggable && (e.button === 0 || e.touches)) {
			const resizerContainer = this.$.overlay.$.resizerContainer;
			const isResizerContainer = e.target === resizerContainer;
			const isResizerContainerScrollbar = e.offsetX > resizerContainer.clientWidth || e.offsetY > resizerContainer.clientHeight;
			const isContentPart = e.target === this.$.overlay.$.content;
			const isDraggable = e.composedPath().some((node, index) => {
				if (!node.classList) return false;
				const isDraggableNode = node.classList.contains(this.__dragHandleClassName || "draggable");
				const isDraggableLeafOnly = node.classList.contains("draggable-leaf-only");
				const isLeafNode = index === 0;
				return isDraggableLeafOnly && isLeafNode || isDraggableNode && (!isDraggableLeafOnly || isLeafNode);
			});
			if (isResizerContainer && !isResizerContainerScrollbar || isContentPart || isDraggable) {
				e.preventDefault();
				this._originalBounds = this.$.overlay.getBounds();
				const event = getMouseOrFirstTouchEvent(e);
				this._originalMouseCoords = {
					top: event.pageY,
					left: event.pageX
				};
				window.addEventListener("mouseup", this._stopDrag);
				window.addEventListener("touchend", this._stopDrag);
				window.addEventListener("mousemove", this._drag);
				window.addEventListener("touchmove", this._drag);
				const { top, left, width, height } = this._originalBounds;
				if (this.$.overlay.$.overlay.style.position !== "absolute") {
					this.top = top;
					this.left = left;
				}
				this.dispatchEvent(new CustomEvent("drag-start", { detail: {
					width,
					height,
					top,
					left
				} }));
			}
		}
	}
	/** @private */
	_drag(e) {
		const event = getMouseOrFirstTouchEvent(e);
		if (eventInWindow(event)) {
			let top = this._originalBounds.top + (event.pageY - this._originalMouseCoords.top);
			let left = this._originalBounds.left + (event.pageX - this._originalMouseCoords.left);
			if (this.keepInViewport) {
				const { width, height } = this._originalBounds;
				const overlayHostBounds = this.$.overlay.getBoundingClientRect();
				const maxLeft = overlayHostBounds.right - overlayHostBounds.left - width;
				const maxTop = overlayHostBounds.bottom - overlayHostBounds.top - height;
				left = Math.max(0, Math.min(left, maxLeft));
				top = Math.max(0, Math.min(top, maxTop));
			}
			this.top = top;
			this.left = left;
		}
	}
	/** @private */
	_stopDrag() {
		this.dispatchEvent(new CustomEvent("dragged", { detail: {
			top: this.top,
			left: this.left
		} }));
		window.removeEventListener("mouseup", this._stopDrag);
		window.removeEventListener("touchend", this._stopDrag);
		window.removeEventListener("mousemove", this._drag);
		window.removeEventListener("touchmove", this._drag);
	}
};
//#endregion
//#region node_modules/@vaadin/dialog/src/vaadin-dialog-renderer-mixin.js
/**
* @license
* Copyright (c) 2017 - 2026 Vaadin Ltd.
* This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
*/
var DialogRendererMixin = (superClass) => class DialogRendererMixin extends superClass {
	static get properties() {
		return {
			/**
			* Custom function for rendering the content of the dialog.
			* Receives two arguments:
			*
			* - `root` The root container DOM element. Append your content to it.
			* - `dialog` The reference to the `<vaadin-dialog>` element.
			* @type {DialogRenderer | undefined}
			*/
			renderer: { type: Object },
			/**
			* String used for rendering a dialog title.
			*
			* If both `headerTitle` and `headerRenderer` are defined, the title
			* and the elements created by the renderer will be placed next to
			* each other, with the title coming first.
			*
			* When `headerTitle` is set, the attribute `has-title` is set on the dialog.
			* @attr {string} header-title
			*/
			headerTitle: { type: String },
			/**
			* Custom function for rendering the dialog header.
			* Receives two arguments:
			*
			* - `root` The root container DOM element. Append your content to it.
			* - `dialog` The reference to the `<vaadin-dialog>` element.
			*
			* If both `headerTitle` and `headerRenderer` are defined, the title
			* and the elements created by the renderer will be placed next to
			* each other, with the title coming first.
			*
			* When `headerRenderer` is set, the attribute `has-header` is set on the dialog.
			* @type {DialogRenderer | undefined}
			*/
			headerRenderer: { type: Object },
			/**
			* Custom function for rendering the dialog footer.
			* Receives two arguments:
			*
			* - `root` The root container DOM element. Append your content to it.
			* - `dialog` The reference to the `<vaadin-dialog>` element.
			*
			* When `footerRenderer` is set, the attribute `has-footer` is set on the dialog.
			* @type {DialogRenderer | undefined}
			*/
			footerRenderer: { type: Object }
		};
	}
	/**
	* Requests an update for the content of the dialog.
	* While performing the update, it invokes the renderer passed in the `renderer` property,
	* as well as `headerRender` and `footerRenderer` properties, if these are defined.
	*
	* It is not guaranteed that the update happens immediately (synchronously) after it is requested.
	*/
	requestContentUpdate() {
		if (this._overlayElement) this._overlayElement.requestContentUpdate();
	}
};
//#endregion
//#region node_modules/@vaadin/dialog/src/vaadin-dialog-resizable-mixin.js
/**
* @license
* Copyright (c) 2017 - 2026 Vaadin Ltd.
* This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
*/
var DialogResizableMixin = (superClass) => class VaadinDialogResizableMixin extends superClass {
	static get properties() {
		return { 
		/**
		* Set to true to enable resizing the dialog by dragging the corners and edges.
		*/
resizable: {
			type: Boolean,
			value: false,
			reflectToAttribute: true
		} };
	}
	/** @protected */
	ready() {
		super.ready();
		this._originalBounds = {};
		this._originalMouseCoords = {};
		this._resizeListeners = {
			start: {},
			resize: {},
			stop: {}
		};
		this._addResizeListeners();
	}
	/** @private */
	_addResizeListeners() {
		[
			"n",
			"e",
			"s",
			"w",
			"nw",
			"ne",
			"se",
			"sw"
		].forEach((direction) => {
			const resizer = document.createElement("div");
			this._resizeListeners.start[direction] = (e) => this._startResize(e, direction);
			this._resizeListeners.resize[direction] = (e) => this._resize(e, direction);
			this._resizeListeners.stop[direction] = () => this._stopResize(direction);
			if (direction.length === 1) resizer.classList.add("edge");
			resizer.classList.add("resizer");
			resizer.classList.add(direction);
			resizer.addEventListener("mousedown", this._resizeListeners.start[direction]);
			resizer.addEventListener("touchstart", this._resizeListeners.start[direction]);
			this.$.overlay.$.resizerContainer.appendChild(resizer);
		});
	}
	/**
	* @param {!MouseEvent | !TouchEvent} e
	* @param {!DialogResizableDirection} direction
	* @protected
	*/
	_startResize(e, direction) {
		if (e.type === "touchstart" && e.touches.length > 1) return;
		if (e.button === 0 || e.touches) {
			e.preventDefault();
			this._originalBounds = this.$.overlay.getBounds();
			const event = getMouseOrFirstTouchEvent(e);
			this._originalMouseCoords = {
				top: event.pageY,
				left: event.pageX
			};
			window.addEventListener("mousemove", this._resizeListeners.resize[direction]);
			window.addEventListener("touchmove", this._resizeListeners.resize[direction]);
			window.addEventListener("mouseup", this._resizeListeners.stop[direction]);
			window.addEventListener("touchend", this._resizeListeners.stop[direction]);
			this.$.overlay.setBounds(this._originalBounds);
			this.$.overlay.setAttribute("has-bounds-set", "");
			const { width, height, top, left } = this._originalBounds;
			this.dispatchEvent(new CustomEvent("resize-start", { detail: {
				width,
				height,
				top,
				left
			} }));
		}
	}
	/**
	* @param {!MouseEvent | !TouchEvent} e
	* @param {!DialogResizableDirection} resizer
	* @protected
	*/
	_resize(e, resizer) {
		const event = getMouseOrFirstTouchEvent(e);
		if (eventInWindow(event)) {
			const minimumSize = 40;
			resizer.split("").forEach((direction) => {
				switch (direction) {
					case "n": {
						const height = this._originalBounds.height - (event.pageY - this._originalMouseCoords.top);
						const top = this._originalBounds.top + (event.pageY - this._originalMouseCoords.top);
						if (height > minimumSize) {
							this.top = top;
							this.height = height;
						}
						break;
					}
					case "e": {
						const width = this._originalBounds.width + (event.pageX - this._originalMouseCoords.left);
						if (width > minimumSize) this.width = width;
						break;
					}
					case "s": {
						const height = this._originalBounds.height + (event.pageY - this._originalMouseCoords.top);
						if (height > minimumSize) this.height = height;
						break;
					}
					case "w": {
						const width = this._originalBounds.width - (event.pageX - this._originalMouseCoords.left);
						const left = this._originalBounds.left + (event.pageX - this._originalMouseCoords.left);
						if (width > minimumSize) {
							this.left = left;
							this.width = width;
						}
						break;
					}
					default: break;
				}
			});
		}
	}
	/**
	* @param {!DialogResizableDirection} direction
	* @protected
	*/
	_stopResize(direction) {
		window.removeEventListener("mousemove", this._resizeListeners.resize[direction]);
		window.removeEventListener("touchmove", this._resizeListeners.resize[direction]);
		window.removeEventListener("mouseup", this._resizeListeners.stop[direction]);
		window.removeEventListener("touchend", this._resizeListeners.stop[direction]);
		this.dispatchEvent(new CustomEvent("resize", { detail: this._getResizeDimensions() }));
	}
	/**
	* @return {!DialogResizeDimensions}
	* @protected
	*/
	_getResizeDimensions() {
		const { width, height, top, left } = getComputedStyle(this.$.overlay.$.overlay);
		return {
			width,
			height,
			top,
			left
		};
	}
};
//#endregion
//#region node_modules/@vaadin/dialog/src/vaadin-dialog.js
/**
* @license
* Copyright (c) 2017 - 2026 Vaadin Ltd.
* This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
*/
/**
* `<vaadin-dialog>` is a Web Component for creating customized modal dialogs.
*
* ### Rendering
*
* The content of the dialog can be populated by using the renderer callback function.
*
* The renderer function provides `root`, `dialog` arguments.
* Generate DOM content, append it to the `root` element and control the state
* of the host element by accessing `dialog`. Before generating new content,
* users are able to check if there is already content in `root` for reusing it.
*
* ```html
* <vaadin-dialog id="dialog"></vaadin-dialog>
* ```
* ```js
* const dialog = document.querySelector('#dialog');
* dialog.renderer = function(root, dialog) {
*   root.textContent = "Sample dialog";
* };
* ```
*
* Renderer is called on the opening of the dialog.
* DOM generated during the renderer call can be reused
* in the next renderer call and will be provided with the `root` argument.
* On first call it will be empty.
*
* ### Styling
*
* The following shadow DOM parts are available for styling:
*
* Part name        | Description
* -----------------|-------------------------------------------
* `backdrop`       | Backdrop of the overlay
* `overlay`        | The overlay container
* `content`        | The overlay content
* `header`         | Element wrapping title and header content
* `header-content` | Element wrapping the header content slot
* `title`          | Element wrapping the title slot
* `footer`         | Element wrapping the footer slot
*
* The following state attributes are available for styling:
*
* Attribute        | Description
* -----------------|--------------------------------------------
* `has-title`      | Set when the element has a title
* `has-header`     | Set when the element has header renderer
* `has-footer`     | Set when the element has footer renderer
* `overflow`       | Set to `top`, `bottom`, none or both
*
* The following custom CSS properties are available for styling:
*
* Custom CSS property                          |
* :--------------------------------------------|
* |`--vaadin-dialog-background`                |
* |`--vaadin-dialog-border-color`              |
* |`--vaadin-dialog-border-radius`             |
* |`--vaadin-dialog-border-width`              |
* |`--vaadin-dialog-max-width`                 |
* |`--vaadin-dialog-min-width`                 |
* |`--vaadin-dialog-overflow-indicator-color`  |
* |`--vaadin-dialog-overflow-indicator-height` |
* |`--vaadin-dialog-padding`                   |
* |`--vaadin-dialog-shadow`                    |
* |`--vaadin-dialog-text-color`                |
* |`--vaadin-dialog-title-color`               |
* |`--vaadin-dialog-title-font-size`           |
* |`--vaadin-dialog-title-font-weight`         |
* |`--vaadin-dialog-title-line-height`         |
* |`--vaadin-overlay-backdrop-background`      |
*
* See [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation.
*
* @fires {CustomEvent} resize-start - Fired when the dialog resize is started.
* @fires {CustomEvent} resize - Fired when the dialog resize is finished.
* @fires {CustomEvent} drag-start - Fired when the dialog drag is started.
* @fires {CustomEvent} dragged - Fired when the dialog drag is finished.
* @fires {CustomEvent} opened-changed - Fired when the `opened` property changes.
* @fires {CustomEvent} closed - Fired when the dialog is closed.
*
* @customElement vaadin-dialog
* @extends HTMLElement
*/
var Dialog = class extends DialogSizeMixin(DialogDraggableMixin(DialogResizableMixin(DialogRendererMixin(DialogBaseMixin(ThemePropertyMixin(ElementMixin(PolylitMixin(LitElement)))))))) {
	static get is() {
		return "vaadin-dialog";
	}
	static get styles() {
		return css`
      :host([opened]),
      :host([opening]),
      :host([closing]) {
        display: block !important;
        position: fixed;
        outline: none;
      }

      :host,
      :host([hidden]) {
        display: none !important;
      }

      :host(:focus-visible) ::part(overlay) {
        outline: var(--vaadin-focus-ring-width) solid var(--vaadin-focus-ring-color);
      }
    `;
	}
	/** @protected */
	render() {
		return html`
      <vaadin-dialog-overlay
        id="overlay"
        .owner="${this}"
        .opened="${this.opened}"
        .headerTitle="${this.headerTitle}"
        .renderer="${this.renderer}"
        .headerRenderer="${this.headerRenderer}"
        .footerRenderer="${this.footerRenderer}"
        .keepInViewport="${this.keepInViewport}"
        @opened-changed="${this._onOverlayOpened}"
        @mousedown="${this._bringOverlayToFront}"
        @touchstart="${this._bringOverlayToFront}"
        theme="${ifDefined(this._theme)}"
        .modeless="${this.modeless}"
        .withBackdrop="${!this.modeless}"
        ?resizable="${this.resizable}"
        restore-focus-on-close
        ?focus-trap="${!this.noFocusTrap}"
        exportparts="backdrop, overlay, header, title, header-content, content, footer"
      >
        <slot name="title" slot="title"></slot>
        <slot name="header-content" slot="header-content"></slot>
        <slot name="footer" slot="footer"></slot>
        <slot></slot>
      </vaadin-dialog-overlay>
    `;
	}
	/** @protected */
	updated(props) {
		super.updated(props);
		if (props.has("headerTitle")) this.ariaLabel = this.headerTitle;
	}
};
defineCustomElement(Dialog);
//#endregion
export { DialogOverlay as n, Dialog as t };

//# sourceMappingURL=vaadin-dialog-BhBV4bdm.js.map