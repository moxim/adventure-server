import { n as get } from "./polylit-mixin-BwU08z1T.js";
import { defineCustomElement } from "./@vaadin_component-base_src_define__js.js";
import { GridColumn } from "./@vaadin_grid_src_vaadin-grid-column__js.js";
import "./vaadin-grid-tree-toggle-CFlW3AkC.js";
//#region node_modules/@vaadin/grid/src/vaadin-grid-tree-column-mixin.js
/**
* @license
* Copyright (c) 2016 - 2026 Vaadin Ltd.
* This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
*/
var GridTreeColumnMixin = (superClass) => class extends superClass {
	static get properties() {
		return { 
		/**
		* JS Path of the property in the item used as text content for the tree toggle.
		*/
path: {
			type: String,
			sync: true
		} };
	}
	static get observers() {
		return ["_onRendererOrBindingChanged(_renderer, _cells, _bodyContentHidden, _cells.*, path)"];
	}
	constructor() {
		super();
		this.__boundOnExpandedChanged = this.__onExpandedChanged.bind(this);
	}
	/**
	* Renders the grid tree toggle to the body cell
	*
	* @private
	*/
	__defaultRenderer(root, _column, { item, expanded, level, hasChildren }) {
		let toggle = root.firstElementChild;
		if (!toggle) {
			toggle = document.createElement("vaadin-grid-tree-toggle");
			toggle.addEventListener("expanded-changed", this.__boundOnExpandedChanged);
			root.appendChild(toggle);
		}
		toggle.__item = item;
		toggle.__rendererExpanded = expanded;
		toggle.expanded = expanded;
		toggle.leaf = !hasChildren;
		const content = this.__getToggleContent(this.path, item);
		if (toggle.textContent !== content) toggle.textContent = content;
		toggle.level = level;
	}
	/**
	* The tree column doesn't allow to use a custom renderer
	* to override the content of body cells.
	* It always renders the grid tree toggle to body cells.
	*
	* @override
	*/
	_computeRenderer() {
		return this.__defaultRenderer;
	}
	/**
	* Expands or collapses the row once the tree toggle is switched.
	* The listener handles only user-fired events.
	*
	* @private
	*/
	__onExpandedChanged(e) {
		if (e.detail.value === e.target.__rendererExpanded) return;
		if (e.detail.value) this._grid.expandItem(e.target.__item);
		else this._grid.collapseItem(e.target.__item);
	}
	/** @private */
	__getToggleContent(path, item) {
		return path && get(path, item);
	}
};
//#endregion
//#region node_modules/@vaadin/grid/src/vaadin-grid-tree-column.js
/**
* @license
* Copyright (c) 2016 - 2026 Vaadin Ltd.
* This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
*/
/**
* `<vaadin-grid-tree-column>` is a helper element for the `<vaadin-grid>`
* that provides default renderer and functionality for toggling tree/hierarchical items.
*
* #### Example:
* ```html
* <vaadin-grid>
*  <vaadin-grid-tree-column path="name.first"></vaadin-grid-tree-column>
*
*  <vaadin-grid-column>
*    ...
* ```
* @customElement vaadin-grid-tree-column
* @extends GridColumn
*/
var GridTreeColumn = class extends GridTreeColumnMixin(GridColumn) {
	static get is() {
		return "vaadin-grid-tree-column";
	}
};
defineCustomElement(GridTreeColumn);
//#endregion
export { GridTreeColumn };

//# sourceMappingURL=@vaadin_grid_vaadin-grid-tree-column__js.js.map