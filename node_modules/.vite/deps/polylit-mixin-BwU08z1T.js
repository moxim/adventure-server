import { r as notEqual } from "./reactive-element-CKo3LdqU.js";
import "./lit.js";
//#region node_modules/@open-wc/dedupe-mixin/src/dedupeMixin.js
var appliedClassMixins = /* @__PURE__ */ new WeakMap();
/** Vefify if the Mixin was previously applyed
* @private
* @param {function} mixin      Mixin being applyed
* @param {object} superClass   Class receiving the new mixin
* @returns {boolean}
*/
function wasMixinPreviouslyApplied(mixin, superClass) {
	let klass = superClass;
	while (klass) {
		if (appliedClassMixins.get(klass) === mixin) return true;
		klass = Object.getPrototypeOf(klass);
	}
	return false;
}
/** Apply each mixin in the chain to make sure they are not applied more than once to the final class.
* @export
* @param {function} mixin      Mixin to be applyed
* @returns {object}            Mixed class with mixin applied
*/
function dedupeMixin(mixin) {
	return (superClass) => {
		if (wasMixinPreviouslyApplied(mixin, superClass)) return superClass;
		const mixedClass = mixin(superClass);
		appliedClassMixins.set(mixedClass, mixin);
		return mixedClass;
	};
}
//#endregion
//#region node_modules/@vaadin/component-base/src/path-utils.js
/**
* @license
* Copyright (c) 2023 - 2026 Vaadin Ltd.
* This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
*/
/**
* Convenience method for reading a value from a path.
*
* @param {string} path
* @param {object} object
*/
function get(path, object) {
	return path.split(".").reduce((obj, property) => obj ? obj[property] : void 0, object);
}
/**
* Convenience method for setting a value to a path.
*
* @param {string} path
* @param {unknown} value
* @param {object} object
*/
function set(path, value, object) {
	const pathParts = path.split(".");
	const lastPart = pathParts.pop();
	const target = pathParts.reduce((target, part) => target[part], object);
	target[lastPart] = value;
}
//#endregion
//#region node_modules/@vaadin/component-base/src/polylit-mixin.js
/**
* @license
* Copyright (c) 2021 - 2026 Vaadin Ltd.
* This program is available under Apache License Version 2.0, available at https://vaadin.com/license/
*/
var caseMap = {};
var CAMEL_TO_DASH = /([A-Z])/gu;
function camelToDash(camel) {
	if (!caseMap[camel]) caseMap[camel] = camel.replace(CAMEL_TO_DASH, "-$1").toLowerCase();
	return caseMap[camel];
}
function upper(name) {
	return name[0].toUpperCase() + name.substring(1);
}
function parseObserver(observerString) {
	const [method, rest] = observerString.split("(");
	return {
		method,
		observerProps: rest.replace(")", "").split(",").map((prop) => prop.trim())
	};
}
function getOrCreateMap(obj, name) {
	if (!Object.prototype.hasOwnProperty.call(obj, name)) obj[name] = new Map(obj[name]);
	return obj[name];
}
var PolylitMixinImplementation = (superclass) => {
	class PolylitMixinClass extends superclass {
		static enabledWarnings = [];
		static createProperty(name, options) {
			if ([
				String,
				Boolean,
				Number,
				Array
			].includes(options)) options = { type: options };
			if (options?.reflectToAttribute) options.reflect = true;
			super.createProperty(name, options);
		}
		static getOrCreateMap(name) {
			return getOrCreateMap(this, name);
		}
		/**
		* @protected
		* @override
		*/
		static finalize() {
			if (window.litIssuedWarnings) {
				window.litIssuedWarnings.add("no-override-create-property");
				window.litIssuedWarnings.add("no-override-get-property-descriptor");
			}
			super.finalize();
			if (Array.isArray(this.observers)) {
				const complexObservers = this.getOrCreateMap("__complexObservers");
				this.observers.forEach((observer) => {
					const { method, observerProps } = parseObserver(observer);
					complexObservers.set(method, observerProps);
				});
			}
		}
		static addCheckedInitializer(initializer) {
			super.addInitializer((instance) => {
				if (instance instanceof this) initializer(instance);
			});
		}
		static getPropertyDescriptor(name, key, options) {
			const defaultDescriptor = super.getPropertyDescriptor(name, key, options);
			let result = defaultDescriptor;
			this.getOrCreateMap("__propKeys").set(name, key);
			if (options.sync) result = {
				get: defaultDescriptor.get,
				set(value) {
					const oldValue = this[name];
					if (notEqual(value, oldValue)) {
						this[key] = value;
						this.requestUpdate(name, oldValue, options);
						if (this.hasUpdated) this.performUpdate();
					}
				},
				configurable: true,
				enumerable: true
			};
			if (options.readOnly) {
				const setter = result.set;
				this.addCheckedInitializer((instance) => {
					instance[`_set${upper(name)}`] = function(value) {
						setter.call(instance, value);
					};
				});
				result = {
					get: result.get,
					set() {},
					configurable: true,
					enumerable: true
				};
			}
			if ("value" in options) this.addCheckedInitializer((instance) => {
				const value = typeof options.value === "function" ? options.value.call(instance) : options.value;
				if (options.readOnly) instance[`_set${upper(name)}`](value);
				else instance[name] = value;
			});
			if (options.observer) {
				const method = options.observer;
				this.getOrCreateMap("__observers").set(name, method);
				this.addCheckedInitializer((instance) => {
					if (!instance[method]) console.warn(`observer method ${method} not defined`);
				});
			}
			if (options.notify) {
				if (!this.__notifyProps) this.__notifyProps = /* @__PURE__ */ new Set();
				else if (!this.hasOwnProperty("__notifyProps")) {
					const notifyProps = this.__notifyProps;
					this.__notifyProps = new Set(notifyProps);
				}
				this.__notifyProps.add(name);
			}
			if (options.computed) {
				const assignComputedMethod = `__assignComputed${name}`;
				const observer = parseObserver(options.computed);
				this.prototype[assignComputedMethod] = function(...props) {
					this[name] = this[observer.method](...props);
				};
				this.getOrCreateMap("__computedObservers").set(assignComputedMethod, observer.observerProps);
			}
			if (!options.attribute) options.attribute = camelToDash(name);
			return result;
		}
		static get polylitConfig() {
			return { asyncFirstRender: false };
		}
		/** @protected */
		connectedCallback() {
			super.connectedCallback();
			const { polylitConfig } = this.constructor;
			if (!this.hasUpdated && !polylitConfig.asyncFirstRender) this.performUpdate();
		}
		/** @protected */
		firstUpdated() {
			super.firstUpdated();
			if (!this.$) this.$ = {};
			this.renderRoot.querySelectorAll("[id]").forEach((node) => {
				this.$[node.id] = node;
			});
		}
		/** @protected */
		ready() {}
		/** @protected */
		willUpdate(props) {
			if (this.constructor.__computedObservers) this.__runComplexObservers(props, this.constructor.__computedObservers);
		}
		/** @protected */
		updated(props) {
			const wasReadyInvoked = this.__isReadyInvoked;
			this.__isReadyInvoked = true;
			if (this.constructor.__observers) this.__runObservers(props, this.constructor.__observers);
			if (this.constructor.__complexObservers) this.__runComplexObservers(props, this.constructor.__complexObservers);
			if (this.__dynamicPropertyObservers) this.__runDynamicObservers(props, this.__dynamicPropertyObservers);
			if (this.__dynamicMethodObservers) this.__runComplexObservers(props, this.__dynamicMethodObservers);
			if (this.constructor.__notifyProps) this.__runNotifyProps(props, this.constructor.__notifyProps);
			if (!wasReadyInvoked) this.ready();
		}
		/**
		* Set several properties at once and perform synchronous update.
		* @protected
		*/
		setProperties(props) {
			Object.entries(props).forEach(([name, value]) => {
				const key = this.constructor.__propKeys.get(name);
				const oldValue = this[key];
				this[key] = value;
				this.requestUpdate(name, oldValue);
			});
			if (this.hasUpdated) this.performUpdate();
		}
		/** @protected */
		_createMethodObserver(observer) {
			const dynamicObservers = getOrCreateMap(this, "__dynamicMethodObservers");
			const { method, observerProps } = parseObserver(observer);
			dynamicObservers.set(method, observerProps);
		}
		/** @protected */
		_createPropertyObserver(property, method) {
			getOrCreateMap(this, "__dynamicPropertyObservers").set(method, property);
		}
		/** @private */
		__runComplexObservers(props, observers) {
			observers.forEach((observerProps, method) => {
				if (observerProps.some((prop) => props.has(prop))) if (!this[method]) console.warn(`observer method ${method} not defined`);
				else this[method](...observerProps.map((prop) => this[prop]));
			});
		}
		/** @private */
		__runDynamicObservers(props, observers) {
			observers.forEach((prop, method) => {
				if (props.has(prop) && this[method]) this[method](this[prop], props.get(prop));
			});
		}
		/** @private */
		__runObservers(props, observers) {
			props.forEach((v, k) => {
				const observer = observers.get(k);
				if (observer !== void 0 && this[observer]) this[observer](this[k], v);
			});
		}
		/** @private */
		__runNotifyProps(props, notifyProps) {
			props.forEach((_, k) => {
				if (notifyProps.has(k)) this.dispatchEvent(new CustomEvent(`${camelToDash(k)}-changed`, { detail: { value: this[k] } }));
			});
		}
		/** @protected */
		_get(path, object) {
			return get(path, object);
		}
		/** @protected */
		_set(path, value, object) {
			set(path, value, object);
		}
	}
	return PolylitMixinClass;
};
var PolylitMixin = dedupeMixin(PolylitMixinImplementation);
//#endregion
export { dedupeMixin as i, get as n, set as r, PolylitMixin as t };

//# sourceMappingURL=polylit-mixin-BwU08z1T.js.map